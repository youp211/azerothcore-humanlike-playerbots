# Nested git clones (the fork + modules live in their own repos; our changes
# to them are captured as patch files under patches/)
azerothcore-wotlk/

# Runtime install: binaries, configs with passwords, 5GB of client data, logs
server/

# Build & install logs
*.log

# Fine-tune artifacts (datasets are generated; models are huge)
finetune/output/
finetune/*.gguf

# WoW game client folders and download parts (user's, 50+ GB)
wotlk*

.env

__pycache__/
*.pyc
*.pyo
