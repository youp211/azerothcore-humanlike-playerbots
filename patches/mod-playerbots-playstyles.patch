diff --git a/conf/playerbots.conf.dist b/conf/playerbots.conf.dist
index a03b13e3..b4cb310e 100644
--- a/conf/playerbots.conf.dist
+++ b/conf/playerbots.conf.dist
@@ -174,6 +174,102 @@ AiPlayerbot.RandomBotGuildSizeMax = 15
 # Delete all randombot guilds if set to 1
 AiPlayerbot.DeleteRandomBotGuilds = 0
 
+###################################################################################################
+# PERSONALITY-DRIVEN GUILDS (EMERGENT FORMATION)
+#
+# Themed guilds (raid / pvp / casual) form GRADUALLY as the world evolves, rather than in one batch
+# at realm start. A few seed guilds are founded at startup (SeedCount); thereafter, every
+# FoundIntervalSec, one more guild is founded while the number of random-bot guilds is below
+# TargetCount. Each founding picks a leader by mod-ollama-chat personality and seeds the guild SMALL
+# (FoundInitialMembers), leaving headroom for the ongoing recruiter (below) to grow it over time and
+# for the real player to join. Elite (raid/pvp) guilds admit only personality-fit members; casual
+# guilds admit anyone with a join chance. Guild size is capped by AiPlayerbot.RandomBotGuildSizeMax.
+###################################################################################################
+
+# Number of guilds founded immediately at realm start (the rest emerge over time)
+AiPlayerbot.PersonalityGuild.SeedCount = 2
+
+# Stop founding new guilds once this many random-bot guilds exist
+AiPlayerbot.PersonalityGuild.TargetCount = 12
+
+# Seconds between emergent-founding attempts (0 disables ongoing founding)
+AiPlayerbot.PersonalityGuild.FoundIntervalSec = 300
+
+# Members a newly-founded guild starts with, INCLUDING the leader (kept small on purpose;
+# the ongoing recruiter grows the guild the rest of the way toward RandomBotGuildSizeMax)
+AiPlayerbot.PersonalityGuild.FoundInitialMembers = 4
+
+# Relative weights for the archetype of each founded guild (Raid : Pvp : Casual). These no longer
+# set fixed guild counts - they only bias which archetype the next guild will be. Set all to 0 to
+# fall back to the built-in ~1:1:2 mix.
+AiPlayerbot.PersonalityGuild.RaidCount = 4
+AiPlayerbot.PersonalityGuild.PvpCount = 3
+AiPlayerbot.PersonalityGuild.CasualCount = 8
+
+# Minimum level a bot must be to lead a personality guild
+AiPlayerbot.PersonalityGuild.LeaderMinLevel = 1
+
+# Per-bot chance (0-100) that an eligible bot rolls into a casual guild at founding
+AiPlayerbot.PersonalityGuild.CasualJoinChance = 40.0
+
+###################################################################################################
+# ONGOING OPPORTUNISTIC GUILD RECRUITMENT
+#
+# During normal play, a bot that FOUNDED a guild (it is the leader) and has a leader/interactive
+# personality keeps recruiting: it periodically sends guild invites to nearby unguilded bots and
+# to the nearby unguilded real player, in its own voice. Elite (raid/pvp) guilds only admit bots
+# whose personality fits the guild (purity preserved); casual guilds admit anyone. Invited bots
+# auto-accept; the real player chooses. Guild size is capped by AiPlayerbot.RandomBotGuildSizeMax.
+###################################################################################################
+
+# Master switch for ongoing opportunistic guild recruitment
+AiPlayerbot.OngoingRecruitEnabled = 1
+
+# Seconds between recruitment passes over online guild-leader bots
+AiPlayerbot.OngoingRecruitIntervalSec = 180
+
+# Yard radius scanned around a recruiting leader for candidates
+AiPlayerbot.OngoingRecruitRange = 30.0
+
+# Minutes before the same target may be re-invited by the same recruiter
+AiPlayerbot.OngoingRecruitCooldownMin = 120
+
+# Chance bonus (0-100) added when the recruiter's sentiment toward the player is high (>= 0.6)
+AiPlayerbot.OngoingRecruitSentimentBonus = 15
+AiPlayerbot.OngoingRecruitPlayerCooldownMin = 30
+
+###################################################################################################
+# PARTY -> GUILD EMERGENT FORMATION
+#
+# When the real player groups with random bots and the run goes WELL, that party can spontaneously
+# become a guild. Every group that contains the real player plus at least one random bot is tracked
+# from the moment it qualifies: how long it has lived and how many member deaths it has suffered.
+# A party becomes guild-eligible once it has been alive at least MinMinutes and the outcome is good -
+# a formation chance that STARTS at BaseChance and is reduced by DeathPenalty per death (floored at
+# 0), so a clean, long-lived party is far likelier to guild up than a wiped one.
+#
+# Two triggers: (1) a periodic world-thread tick (every CheckIntervalSec) rolls the outcome-adjusted
+# chance; (2) the player says a "let's guild up" line in party chat (routed from mod-ollama-chat),
+# which bypasses the random roll but still requires the MinMinutes + not-wiped gate - otherwise a
+# party bot declines in-voice. On formation, the most leader-suited bot founds the guild and invites
+# the whole crew (bots auto-accept; the player chooses); the founder's personality names the guild.
+###################################################################################################
+
+# Master switch for party -> guild emergent formation
+AiPlayerbot.PartyGuild.Enabled = 1
+
+# Minutes a party must have been alive before it can become a guild
+AiPlayerbot.PartyGuild.MinMinutes = 5
+
+# Starting formation chance (0-100) for a clean party with no deaths
+AiPlayerbot.PartyGuild.BaseChance = 8.0
+
+# Chance reduction per member death during the party's life (floored at 0)
+AiPlayerbot.PartyGuild.DeathPenalty = 6.0
+
+# Seconds between spontaneous-formation evaluation ticks
+AiPlayerbot.PartyGuild.CheckIntervalSec = 120
+
 # Randombots will invite players to groups/raids/guilds
 AiPlayerbot.RandomBotInvitePlayer = 0
 
@@ -2351,3 +2447,19 @@ AiPlayerbot.TargetPosRecalcDistance = 0.1
 
 # Allow bots to be summoned near innkeepers
 AiPlayerbot.SummonAtInnkeepersEnabled = 1
+
+###################################################################################################
+# QUEST-HELP INVITES (local patch)
+# Ungrouped bots in good sentiment standing (mod-ollama-chat) occasionally invite a nearby real
+# player to group - most likely when they confirm a shared quest. Chances are % per 30s check.
+AiPlayerbot.QuestHelpSentimentThreshold = 0.6
+AiPlayerbot.QuestHelpConfirmedChance = 2.0
+AiPlayerbot.QuestHelpNearbyChance = 0.5
+AiPlayerbot.QuestHelpRandomChance = 0.1
+
+###################################################################################################
+# ORGANIC BOT ECONOMY (local patch)
+# Bots list real spare items on the auction house when near an auctioneer, priced by personality
+# (elitists overprice, clueless underprice). % per check while near an auctioneer; per-bot cap.
+AiPlayerbot.BotAhSellChance = 25.0
+AiPlayerbot.BotAhSellMaxListings = 8
diff --git a/src/Ai/Base/ActionContext.h b/src/Ai/Base/ActionContext.h
index 7b95e4b0..b7ce1036 100644
--- a/src/Ai/Base/ActionContext.h
+++ b/src/Ai/Base/ActionContext.h
@@ -123,6 +123,7 @@ public:
         creators["dps assist"] = &ActionContext::dps_assist;
         creators["dps aoe"] = &ActionContext::dps_aoe;
         creators["attack rti target"] = &ActionContext::attack_rti_target;
+        creators["attack arena kill target"] = &ActionContext::attack_arena_kill_target;
         creators["loot"] = &ActionContext::loot;
         creators["add loot"] = &ActionContext::add_loot;
         creators["add gathering loot"] = &ActionContext::add_gathering_loot;
@@ -275,6 +276,8 @@ public:
         creators["new rpg wander random"] = &ActionContext::new_rpg_wander_random;
         creators["new rpg wander npc"] = &ActionContext::new_rpg_wander_npc;
         creators["new rpg do quest"] = &ActionContext::new_rpg_do_quest;
+        creators["offer quest help"] = &ActionContext::offer_quest_help;
+        creators["ah sell spares"] = &ActionContext::ah_sell_spares;
         creators["new rpg travel flight"] = &ActionContext::new_rpg_travel_flight;
         creators["new rpg outdoor pvp"] = &ActionContext::new_rpg_outdoor_pvp;
         creators["wait for attack keep safe distance"] = &ActionContext::wait_for_attack_keep_safe_distance;
@@ -357,6 +360,7 @@ private:
     static Action* dps_assist(PlayerbotAI* botAI) { return new DpsAssistAction(botAI); }
     static Action* dps_aoe(PlayerbotAI* botAI) { return new DpsAoeAction(botAI); }
     static Action* attack_rti_target(PlayerbotAI* botAI) { return new AttackRtiTargetAction(botAI); }
+    static Action* attack_arena_kill_target(PlayerbotAI* botAI) { return new AttackArenaKillTargetAction(botAI); }
     static Action* tank_assist(PlayerbotAI* botAI) { return new TankAssistAction(botAI); }
     static Action* drink(PlayerbotAI* botAI) { return new DrinkAction(botAI); }
     static Action* food(PlayerbotAI* botAI) { return new EatAction(botAI); }
@@ -482,6 +486,8 @@ private:
     static Action* new_rpg_wander_random(PlayerbotAI* ai) { return new NewRpgWanderRandomAction(ai); }
     static Action* new_rpg_wander_npc(PlayerbotAI* ai) { return new NewRpgWanderNpcAction(ai); }
     static Action* new_rpg_do_quest(PlayerbotAI* ai) { return new NewRpgDoQuestAction(ai); }
+    static Action* offer_quest_help(PlayerbotAI* ai) { return new OfferQuestHelpAction(ai); }
+    static Action* ah_sell_spares(PlayerbotAI* ai) { return new AhSellSparesAction(ai); }
     static Action* new_rpg_travel_flight(PlayerbotAI* ai) { return new NewRpgTravelFlightAction(ai); }
     static Action* new_rpg_outdoor_pvp(PlayerbotAI* ai) { return new NewRpgOutdoorPvpAction(ai); }
     static Action* wait_for_attack_keep_safe_distance(PlayerbotAI* ai) { return new WaitForAttackKeepSafeDistanceAction(ai); }
diff --git a/src/Ai/Base/Actions/BattleGroundTactics.cpp b/src/Ai/Base/Actions/BattleGroundTactics.cpp
index 238a2291..aa4f9fc7 100644
--- a/src/Ai/Base/Actions/BattleGroundTactics.cpp
+++ b/src/Ai/Base/Actions/BattleGroundTactics.cpp
@@ -4430,3 +4430,13 @@ bool ArenaTactics::moveToCenter(Battleground* bg)
 
     return true;
 }
+
+bool ArenaBurstSyncAction::Execute(Event /*event*/)
+{
+    // The trigger fires only when the boost strategy state disagrees with the
+    // team burst window, so flipping it reconciles the two.
+    bool bursting = botAI->HasStrategy("boost", BOT_STATE_COMBAT);
+    botAI->ChangeStrategy(bursting ? "-boost" : "+boost", BOT_STATE_COMBAT);
+    LOG_DEBUG("playerbots", "[Arena] Bot {} {} burst window", bot->GetName(), bursting ? "leaves" : "enters");
+    return true;
+}
diff --git a/src/Ai/Base/Actions/BattleGroundTactics.h b/src/Ai/Base/Actions/BattleGroundTactics.h
index 4c0072df..e3a1a596 100644
--- a/src/Ai/Base/Actions/BattleGroundTactics.h
+++ b/src/Ai/Base/Actions/BattleGroundTactics.h
@@ -144,4 +144,15 @@ private:
     bool moveToCenter(Battleground* bg);
 };
 
+// Toggles the boost (burst cooldown) strategy to match the team burst window
+// (see ArenaBurstWindowTrigger): cooldowns are held until a teammate bursts or
+// the kill target hits execute range, then the whole team unloads together.
+class ArenaBurstSyncAction : public Action
+{
+public:
+    ArenaBurstSyncAction(PlayerbotAI* botAI) : Action(botAI, "arena burst sync") {}
+
+    bool Execute(Event event) override;
+};
+
 #endif
diff --git a/src/Ai/Base/Actions/ChooseTargetActions.cpp b/src/Ai/Base/Actions/ChooseTargetActions.cpp
index debcfac0..18d0386f 100644
--- a/src/Ai/Base/Actions/ChooseTargetActions.cpp
+++ b/src/Ai/Base/Actions/ChooseTargetActions.cpp
@@ -189,3 +189,15 @@ bool AttackRtiTargetAction::isUseful()
 
     return true;
 }
+
+bool AttackArenaKillTargetAction::isUseful()
+{
+    if (!bot->InArena())
+        return false;
+
+    if (botAI->ContainsStrategy(STRATEGY_TYPE_HEAL))
+        return false;
+
+    Unit* killTarget = AI_VALUE(Unit*, "arena kill target");
+    return killTarget && bot->GetVictim() != killTarget;
+}
diff --git a/src/Ai/Base/Actions/ChooseTargetActions.h b/src/Ai/Base/Actions/ChooseTargetActions.h
index 21a24aa3..385fb660 100644
--- a/src/Ai/Base/Actions/ChooseTargetActions.h
+++ b/src/Ai/Base/Actions/ChooseTargetActions.h
@@ -91,6 +91,15 @@ public:
     bool isUseful() override;
 };
 
+class AttackArenaKillTargetAction : public AttackAction
+{
+public:
+    AttackArenaKillTargetAction(PlayerbotAI* botAI) : AttackAction(botAI, "attack arena kill target") {}
+
+    std::string const GetTargetName() override { return "arena kill target"; }
+    bool isUseful() override;
+};
+
 class DropTargetAction : public Action
 {
 public:
diff --git a/src/Ai/Base/Actions/InviteToGroupAction.cpp b/src/Ai/Base/Actions/InviteToGroupAction.cpp
index e7bcb4da..8d0869b2 100644
--- a/src/Ai/Base/Actions/InviteToGroupAction.cpp
+++ b/src/Ai/Base/Actions/InviteToGroupAction.cpp
@@ -6,12 +6,26 @@
 #include "InviteToGroupAction.h"
 
 #include "BroadcastHelper.h"
+#include "DatabaseEnv.h"
 #include "Event.h"
 #include "GuildMgr.h"
+#include "ObjectMgr.h"
 #include "PlayerbotOperations.h"
 #include "Playerbots.h"
 #include "PlayerbotWorldThreadProcessor.h"
 #include "ServerFacade.h"
+#include "StringFormat.h"
+#include "Timer.h"
+
+#include <mutex>
+
+// Provided by mod-ollama-chat (same binary): bot speaks an LLM-generated line.
+[[gnu::weak]] void OllamaChat_SpeakSituation(Player* bot, Player* target, std::string const& situation, bool whisper);
+static void SpeakSituation(Player* bot, Player* target, std::string const& situation, bool whisper)
+{
+    if (OllamaChat_SpeakSituation)
+        OllamaChat_SpeakSituation(bot, target, situation, whisper);
+}
 
 bool InviteToGroupAction::Invite(Player* inviter, Player* player)
 {
@@ -456,3 +470,103 @@ bool LfgAction::Execute(Event event)
 
     return false;
 }
+
+// Sentiment bridge to mod-ollama-chat (shared characters DB, guid counters):
+// cached per pair, neutral 0.5 when the chat module or the row is absent.
+static float GetBotPlayerSentiment(Player* bot, Player* player)
+{
+    static int hasTable = -1;
+    if (hasTable == -1)
+        hasTable = CharacterDatabase.Query(
+            "SELECT 1 FROM information_schema.TABLES WHERE TABLE_SCHEMA = DATABASE() "
+            "AND TABLE_NAME = 'mod_ollama_chat_bot_player_sentiments'") ? 1 : 0;
+    if (!hasTable)
+        return 0.5f;
+
+    static std::mutex sentimentCacheMutex;
+    static std::map<std::pair<ObjectGuid::LowType, ObjectGuid::LowType>, std::pair<float, uint32>> sentimentCache;
+    auto key = std::make_pair(bot->GetGUID().GetCounter(), player->GetGUID().GetCounter());
+    uint32 now = getMSTime();
+    {
+        std::lock_guard<std::mutex> lock(sentimentCacheMutex);
+        auto it = sentimentCache.find(key);
+        if (it != sentimentCache.end() && now - it->second.second < 5 * MINUTE * IN_MILLISECONDS)
+            return it->second.first;
+    }
+
+    float value = 0.5f;
+    QueryResult result = CharacterDatabase.Query(
+        "SELECT sentiment_value FROM mod_ollama_chat_bot_player_sentiments WHERE bot_guid = {} AND player_guid = {}",
+        key.first, key.second);
+    if (result)
+        value = (*result)[0].Get<float>();
+
+    std::lock_guard<std::mutex> lock(sentimentCacheMutex);
+    sentimentCache[key] = {value, now};
+    return value;
+}
+
+Player* OfferQuestHelpAction::FindQuestHelpTarget(Player* bot, PlayerbotAI* botAI, int& tier)
+{
+    if (!bot->IsAlive() || bot->GetGroup() || bot->InBattleground() || bot->IsInCombat())
+        return nullptr;
+
+    uint32 questId = 0;
+    if (botAI->rpgInfo.GetStatus() == RPG_DO_QUEST)
+        questId = std::get<NewRpgInfo::DoQuest>(botAI->rpgInfo.data).questId;
+
+    GuidVector nearGuids = botAI->GetAiObjectContext()->GetValue<GuidVector>("nearest friendly players")->Get();
+    for (ObjectGuid const& guid : nearGuids)
+    {
+        Player* player = ObjectAccessor::FindPlayer(guid);
+        if (!player || player == bot || !player->IsAlive() || player->GetGroup())
+            continue;
+        if (GET_PLAYERBOT_AI(player))
+            continue; // offer to real players; bots group up via other features
+        if (player->isDND() || player->IsBeingTeleported())
+            continue;
+        if (bot->GetDistance(player) > 30.0f)
+            continue;
+        if (GetBotPlayerSentiment(bot, player) < sPlayerbotAIConfig.questHelpSentimentThreshold)
+            continue;
+
+        if (questId && player->GetQuestStatus(questId) == QUEST_STATUS_INCOMPLETE)
+            tier = 2; // confirmed: same quest in both logs
+        else if (questId)
+            tier = 1; // bot is questing here, can't confirm the player is
+        else
+            tier = 0; // not questing - the rare generic help offer
+        return player;
+    }
+
+    return nullptr;
+}
+
+bool OfferQuestHelpAction::Execute(Event /*event*/)
+{
+    int tier = 0;
+    Player* target = FindQuestHelpTarget(bot, botAI, tier);
+    if (!target)
+        return false;
+
+    std::string situation;
+    if (tier == 2)
+    {
+        uint32 questId = std::get<NewRpgInfo::DoQuest>(botAI->rpgInfo.data).questId;
+        Quest const* quest = sObjectMgr->GetQuestTemplate(questId);
+        std::string title = quest ? quest->GetTitle() : "the same quest";
+        situation = Acore::StringFormat("you and this player are both on the quest '{}'; you're inviting them to group up and finish it faster", title);
+    }
+    else if (tier == 1)
+        situation = "this player is questing near you; you're inviting them to group up";
+    else
+        situation = "you're offering this nearby player a hand with whatever they're doing";
+
+    // in-character invite line via the LLM (falls back silently if chat off)
+    SpeakSituation(bot, target, situation, false);
+    bool invited = Invite(bot, target);
+    if (invited)
+        LOG_DEBUG("playerbots", "[QuestHelp] Bot {} offered quest help to {} (tier {})",
+                  bot->GetName(), target->GetName(), tier);
+    return invited;
+}
diff --git a/src/Ai/Base/Actions/InviteToGroupAction.h b/src/Ai/Base/Actions/InviteToGroupAction.h
index 02162ef3..a9998114 100644
--- a/src/Ai/Base/Actions/InviteToGroupAction.h
+++ b/src/Ai/Base/Actions/InviteToGroupAction.h
@@ -32,6 +32,20 @@ public:
     bool Execute(Event event) override;
 };
 
+// Bot-initiated quest help: invite a nearby real player the bot is in good
+// sentiment standing with (see QuestHelpOfferTrigger for the odds). Tier 2 =
+// bot confirmed the player shares its active quest, tier 1 = bot is questing
+// but can't confirm, tier 0 = generic rare help offer.
+class OfferQuestHelpAction : public InviteToGroupAction
+{
+public:
+    OfferQuestHelpAction(PlayerbotAI* botAI) : InviteToGroupAction(botAI, "offer quest help") {}
+
+    bool Execute(Event event) override;
+
+    static Player* FindQuestHelpTarget(Player* bot, PlayerbotAI* botAI, int& tier);
+};
+
 class InviteNearbyToGroupAction : public InviteToGroupAction
 {
 public:
diff --git a/src/Ai/Base/Actions/TradeStatusAction.cpp b/src/Ai/Base/Actions/TradeStatusAction.cpp
index c3a63ee7..cfbe480e 100644
--- a/src/Ai/Base/Actions/TradeStatusAction.cpp
+++ b/src/Ai/Base/Actions/TradeStatusAction.cpp
@@ -17,6 +17,49 @@
 #include "RandomPlayerbotMgr.h"
 #include "SetCraftAction.h"
 
+#include "DatabaseEnv.h"
+
+// Provided by mod-ollama-chat (same binary): make a bot speak an in-character,
+// LLM-generated line about a situation. Weakly optional - if the chat module
+// is disabled the call is a no-op stub below.
+[[gnu::weak]] void OllamaChat_SpeakSituation(Player* bot, Player* target, std::string const& situation, bool whisper);
+static void SpeakSituation(Player* bot, Player* target, std::string const& situation, bool whisper)
+{
+    if (OllamaChat_SpeakSituation)
+        OllamaChat_SpeakSituation(bot, target, situation, whisper);
+}
+
+// mod-ollama-chat parks in-person gear gives in mod_ollama_chat_pending_gives
+// (the bot promised an item in chat); the hand-over completes here when the
+// player opens a trade window with the bot. Returns parked item guid (0 = none).
+static ObjectGuid::LowType PendingGiveItemGuid(Player* bot, Player* trader, uint32* codOut)
+{
+    static int hasTable = -1;
+    if (hasTable == -1)
+        hasTable = CharacterDatabase.Query(
+            "SELECT 1 FROM information_schema.TABLES WHERE TABLE_SCHEMA = DATABASE() "
+            "AND TABLE_NAME = 'mod_ollama_chat_pending_gives'") ? 1 : 0;
+    if (!hasTable)
+        return 0;
+
+    QueryResult result = CharacterDatabase.Query(
+        "SELECT item_guid, cod FROM mod_ollama_chat_pending_gives "
+        "WHERE bot_guid = {} AND player_guid = {} AND created_at > NOW() - INTERVAL 10 MINUTE",
+        bot->GetGUID().GetCounter(), trader->GetGUID().GetCounter());
+    if (!result)
+        return 0;
+
+    if (codOut)
+        *codOut = (*result)[1].Get<uint32>();
+    return (*result)[0].Get<ObjectGuid::LowType>();
+}
+
+static void ClearPendingGive(Player* bot, Player* trader)
+{
+    CharacterDatabase.Execute("DELETE FROM mod_ollama_chat_pending_gives WHERE bot_guid = {} AND player_guid = {}",
+                              bot->GetGUID().GetCounter(), trader->GetGUID().GetCounter());
+}
+
 bool TradeStatusAction::Execute(Event event)
 {
     Player* trader = bot->GetTrader();
@@ -26,6 +69,12 @@ bool TradeStatusAction::Execute(Event event)
 
     PlayerbotAI* traderBotAI = GET_PLAYERBOT_AI(trader);
 
+    // In-person gear give promised in chat: hand the parked item over
+    uint32 pendingCod = 0;
+    if (!traderBotAI)
+        if (ObjectGuid::LowType pendingItem = PendingGiveItemGuid(bot, trader, &pendingCod))
+            return HandlePendingGearGive(event, trader, pendingItem, pendingCod);
+
     // Allow the master and group members to trade
     if (trader != master && !traderBotAI && (!bot->GetGroup() || !bot->GetGroup()->IsMember(trader->GetGUID())))
     {
@@ -377,3 +426,75 @@ int32 TradeStatusAction::CalculateCost(Player* player, bool sell)
 
     return sum;
 }
+
+bool TradeStatusAction::HandlePendingGearGive(Event& event, Player* trader, ObjectGuid::LowType itemGuidLow, uint32 cod)
+{
+    WorldPacket p(event.getPacket());
+    p.rpos(0);
+    uint32 status;
+    p >> status;
+
+    Item* item = bot->GetItemByGuid(ObjectGuid::Create<HighGuid::Item>(itemGuidLow));
+
+    switch (status)
+    {
+        case TRADE_STATUS_BEGIN_TRADE:
+        {
+            if (!item)
+            {
+                // item vanished (equipped/sold/mailed meanwhile) - apologize and bail
+                ClearPendingGive(bot, trader);
+                SpeakSituation(bot, trader, "you promised them a piece of gear but no longer have it", true);
+                WorldPacket cancel;
+                uint32 cancelStatus = 0;
+                cancel << cancelStatus;
+                bot->GetSession()->HandleCancelTradeOpcode(cancel);
+                return false;
+            }
+            if (!bot->HasInArc(CAST_ANGLE_IN_FRONT, trader, sPlayerbotAIConfig.sightDistance))
+                bot->SetFacingToObject(trader);
+            WorldPacket begin;
+            bot->GetSession()->HandleBeginTradeOpcode(begin);
+            return true;
+        }
+        case TRADE_STATUS_OPEN_WINDOW:
+        {
+            TradeData* myTrade = bot->GetTradeData();
+            if (!myTrade || !item)
+                return false;
+            myTrade->SetItem(TradeSlots(0), item);
+            if (cod)
+                SpeakSituation(bot, trader, Acore::StringFormat(
+                    "you put your {} in the trade window; it costs them {} silver, ask them to add the money",
+                    item->GetTemplate()->Name1, cod / 100), true);
+            WorldPacket accept;
+            uint32 acceptStatus = 0;
+            accept << acceptStatus;
+            bot->GetSession()->HandleAcceptTradeOpcode(accept);
+            return true;
+        }
+        case TRADE_STATUS_TRADE_ACCEPT:
+        case TRADE_STATUS_BACK_TO_TRADE:
+        {
+            // for a paid hand-over, hold acceptance until the money is in
+            if (cod && trader->GetTradeData() && trader->GetTradeData()->GetMoney() < cod)
+                return true;
+            WorldPacket accept;
+            uint32 acceptStatus = 0;
+            accept << acceptStatus;
+            bot->GetSession()->HandleAcceptTradeOpcode(accept);
+            return true;
+        }
+        case TRADE_STATUS_TRADE_COMPLETE:
+        {
+            ClearPendingGive(bot, trader);
+            SpeakSituation(bot, trader, cod ? "you just sold them a piece of gear in a trade, they paid up"
+                                            : "you just gave them a piece of gear for free in a trade", true);
+            LOG_INFO("playerbots", "[GearGive] {} handed item over to {} in trade (cod {} copper)",
+                     bot->GetName(), trader->GetName(), cod);
+            return true;
+        }
+        default:
+            return false;
+    }
+}
diff --git a/src/Ai/Base/Actions/TradeStatusAction.h b/src/Ai/Base/Actions/TradeStatusAction.h
index af481895..a6ec7f4a 100644
--- a/src/Ai/Base/Actions/TradeStatusAction.h
+++ b/src/Ai/Base/Actions/TradeStatusAction.h
@@ -22,6 +22,7 @@ private:
     void BeginTrade();
     bool CheckTrade();
     int32 CalculateCost(Player* player, bool sell);
+    bool HandlePendingGearGive(Event& event, Player* trader, ObjectGuid::LowType itemGuidLow, uint32 cod);
 };
 
 #endif
diff --git a/src/Ai/Base/Strategy/BattlegroundStrategy.cpp b/src/Ai/Base/Strategy/BattlegroundStrategy.cpp
index f414e752..80de4fec 100644
--- a/src/Ai/Base/Strategy/BattlegroundStrategy.cpp
+++ b/src/Ai/Base/Strategy/BattlegroundStrategy.cpp
@@ -84,4 +84,10 @@ void ArenaStrategy::InitTriggers(std::vector<TriggerNode*>& triggers)
 {
     triggers.push_back(
         new TriggerNode("no possible targets", { NextAction("arena tactics", ACTION_BG)}));
+    // team coordination: everyone on the called kill target, cooldowns
+    // held/released together with the team burst window
+    triggers.push_back(
+        new TriggerNode("arena focus", { NextAction("attack arena kill target", ACTION_HIGH + 5.0f)}));
+    triggers.push_back(
+        new TriggerNode("arena burst window", { NextAction("arena burst sync", ACTION_HIGH + 6.0f)}));
 }
diff --git a/src/Ai/Base/Trigger/PvpTriggers.cpp b/src/Ai/Base/Trigger/PvpTriggers.cpp
index b20c5df9..8f8d074d 100644
--- a/src/Ai/Base/Trigger/PvpTriggers.cpp
+++ b/src/Ai/Base/Trigger/PvpTriggers.cpp
@@ -9,6 +9,7 @@
 #include "BattlegroundEY.h"
 #include "BattlegroundMgr.h"
 #include "BattlegroundWS.h"
+#include "ObjectAccessor.h"
 #include "Playerbots.h"
 #include "ServerFacade.h"
 #include "BattlegroundAV.h"
@@ -335,3 +336,75 @@ bool AllianceNoSnowfallGY::IsActive()
 
     return false;
 }
+
+bool ArenaFocusTrigger::IsActive()
+{
+    if (!bot->InArena())
+        return false;
+
+    Unit* killTarget = AI_VALUE(Unit*, "arena kill target");
+    return killTarget && bot->GetVictim() != killTarget;
+}
+
+// Iconic offensive burst cooldowns per class (3.3.5a). Used to detect that a
+// teammate - bot or real player - just opened a burst window.
+static bool HasBurstAura(Player* player)
+{
+    static uint32 const burstAuras[] = {
+        1719,   // Recklessness
+        12292,  // Death Wish
+        31884,  // Avenging Wrath
+        12472,  // Icy Veins
+        12042,  // Arcane Power
+        11129,  // Combustion
+        13750,  // Adrenaline Rush
+        51690,  // Killing Spree
+        51713,  // Shadow Dance
+        19574,  // Bestial Wrath
+        3045,   // Rapid Fire
+        2825,   // Bloodlust
+        32182,  // Heroism
+        16166,  // Elemental Mastery
+        47241,  // Metamorphosis
+        49016,  // Hysteria
+        50334,  // Berserk (feral)
+        49206   // Summon Gargoyle
+    };
+    for (uint32 spellId : burstAuras)
+        if (player->HasAura(spellId))
+            return true;
+
+    return false;
+}
+
+bool ArenaBurstWindowTrigger::IsActive()
+{
+    if (!bot->InArena() || !bot->IsAlive())
+        return false;
+
+    Battleground* bg = bot->GetBattleground();
+    if (!bg || bg->GetStatus() != STATUS_IN_PROGRESS)
+        return false;
+
+    bool window = false;
+    for (auto const& itr : bg->GetPlayers())
+    {
+        Player* member = ObjectAccessor::FindPlayer(itr.first);
+        if (!member || !member->IsAlive() || member->GetBgTeamId() != bot->GetBgTeamId())
+            continue;
+
+        if (HasBurstAura(member))
+        {
+            window = true;
+            break;
+        }
+    }
+
+    if (!window)
+    {
+        Unit* killTarget = AI_VALUE(Unit*, "arena kill target");
+        window = killTarget && killTarget->GetHealthPct() <= 50.0f;
+    }
+
+    return window != botAI->HasStrategy("boost", BOT_STATE_COMBAT);
+}
diff --git a/src/Ai/Base/Trigger/PvpTriggers.h b/src/Ai/Base/Trigger/PvpTriggers.h
index 5077dda7..c1cb77f7 100644
--- a/src/Ai/Base/Trigger/PvpTriggers.h
+++ b/src/Ai/Base/Trigger/PvpTriggers.h
@@ -26,6 +26,25 @@ public:
     bool IsActive() override;
 };
 
+class ArenaFocusTrigger : public Trigger
+{
+public:
+    ArenaFocusTrigger(PlayerbotAI* botAI) : Trigger(botAI, "arena focus", 2) {}
+
+    bool IsActive() override;
+};
+
+// Active when the bot's boost (burst cooldown) strategy state disagrees with
+// the team burst window: a teammate - bot or human - has a burst cooldown
+// running, or the kill target is in execute range.
+class ArenaBurstWindowTrigger : public Trigger
+{
+public:
+    ArenaBurstWindowTrigger(PlayerbotAI* botAI) : Trigger(botAI, "arena burst window", 2) {}
+
+    bool IsActive() override;
+};
+
 // NOTE this trigger is only active when bot is actively returning flag
 // (not when hiding in base because enemy has flag too)
 class PlayerHasFlag : public Trigger
diff --git a/src/Ai/Base/TriggerContext.h b/src/Ai/Base/TriggerContext.h
index 3b30691d..69407cff 100644
--- a/src/Ai/Base/TriggerContext.h
+++ b/src/Ai/Base/TriggerContext.h
@@ -88,6 +88,8 @@ public:
         creators["not dps aoe target active"] = &TriggerContext::not_dps_aoe_target_active;
         creators["has nearest adds"] = &TriggerContext::has_nearest_adds;
         creators["enemy player near"] = &TriggerContext::enemy_player_near;
+        creators["arena focus"] = &TriggerContext::arena_focus;
+        creators["arena burst window"] = &TriggerContext::arena_burst_window;
 
         creators["tank assist"] = &TriggerContext::TankAssist;
         creators["lose aggro"] = &TriggerContext::LoseAggro;
@@ -237,6 +239,8 @@ public:
         creators["wander random status"] = &TriggerContext::wander_random_status;
         creators["wander npc status"] = &TriggerContext::wander_npc_status;
         creators["do quest status"] = &TriggerContext::do_quest_status;
+        creators["quest help offer"] = &TriggerContext::quest_help_offer;
+        creators["ah sell spares"] = &TriggerContext::ah_sell_spares;
         creators["travel flight status"] = &TriggerContext::travel_flight_status;
         creators["outdoor pvp status"] = &TriggerContext::outdoor_pvp_status;
         creators["can self resurrect"] = &TriggerContext::can_self_resurrect;
@@ -338,6 +342,8 @@ private:
     static Trigger* not_dps_aoe_target_active(PlayerbotAI* botAI) { return new NotDpsAoeTargetActiveTrigger(botAI); }
     static Trigger* has_nearest_adds(PlayerbotAI* botAI) { return new HasNearestAddsTrigger(botAI); }
     static Trigger* enemy_player_near(PlayerbotAI* botAI) { return new EnemyPlayerNear(botAI); }
+    static Trigger* arena_focus(PlayerbotAI* botAI) { return new ArenaFocusTrigger(botAI); }
+    static Trigger* arena_burst_window(PlayerbotAI* botAI) { return new ArenaBurstWindowTrigger(botAI); }
     static Trigger* Random(PlayerbotAI* botAI) { return new RandomTrigger(botAI, "random", 20); }
     static Trigger* seldom(PlayerbotAI* botAI) { return new RandomTrigger(botAI, "seldom", 300); }
     static Trigger* often(PlayerbotAI* botAI) { return new RandomTrigger(botAI, "often", 5); }
@@ -451,6 +457,8 @@ private:
     static Trigger* wander_random_status(PlayerbotAI* botAI) { return new NewRpgStatusTrigger(botAI, RPG_WANDER_RANDOM); }
     static Trigger* wander_npc_status(PlayerbotAI* botAI) { return new NewRpgStatusTrigger(botAI, RPG_WANDER_NPC); }
     static Trigger* do_quest_status(PlayerbotAI* botAI) { return new NewRpgStatusTrigger(botAI, RPG_DO_QUEST); }
+    static Trigger* quest_help_offer(PlayerbotAI* botAI) { return new QuestHelpOfferTrigger(botAI); }
+    static Trigger* ah_sell_spares(PlayerbotAI* botAI) { return new AhSellSparesTrigger(botAI); }
     static Trigger* travel_flight_status(PlayerbotAI* botAI) { return new NewRpgStatusTrigger(botAI, RPG_TRAVEL_FLIGHT); }
     static Trigger* outdoor_pvp_status(PlayerbotAI* botAI) { return new NewRpgStatusTrigger(botAI, RPG_OUTDOOR_PVP); }
     static Trigger* can_self_resurrect(PlayerbotAI* ai) { return new SelfResurrectTrigger(ai); }
diff --git a/src/Ai/Base/Value/LeastHpTargetValue.cpp b/src/Ai/Base/Value/LeastHpTargetValue.cpp
index d8579f0d..dd21bbc9 100644
--- a/src/Ai/Base/Value/LeastHpTargetValue.cpp
+++ b/src/Ai/Base/Value/LeastHpTargetValue.cpp
@@ -6,6 +6,8 @@
 #include "LeastHpTargetValue.h"
 
 #include "AttackersValue.h"
+#include "Battleground.h"
+#include "ObjectAccessor.h"
 #include "Playerbots.h"
 
 class FindLeastHpTargetStrategy : public FindNonCcTargetStrategy
@@ -32,3 +34,52 @@ Unit* LeastHpTargetValue::Calculate()
 
     return FindTarget(&strategy);
 }
+
+Unit* ArenaKillTargetValue::Calculate()
+{
+    if (!bot->InArena())
+        return nullptr;
+
+    Battleground* bg = bot->GetBattleground();
+    if (!bg || bg->GetStatus() != STATUS_IN_PROGRESS)
+        return nullptr;
+
+    // A real player on the team calls the kill target just by attacking:
+    // whatever the human is hitting, the bots assist.
+    for (auto const& itr : bg->GetPlayers())
+    {
+        Player* member = ObjectAccessor::FindPlayer(itr.first);
+        if (!member || member->GetBgTeamId() != bot->GetBgTeamId())
+            continue;
+
+        PlayerbotAI* memberAI = GET_PLAYERBOT_AI(member);
+        if (memberAI && !memberAI->IsRealPlayer())
+            continue;
+
+        Unit* victim = member->GetVictim();
+        if (victim && victim->IsAlive() && victim->IsPlayer() &&
+            victim->ToPlayer()->GetBgTeamId() != bot->GetBgTeamId())
+            return victim;
+    }
+
+    // The players map is guid-ordered and health is bucketed, so every member
+    // of the team picks the same enemy and the pick doesn't flap between
+    // similar-health targets mid-fight.
+    Unit* best = nullptr;
+    uint32 bestScore = 0xFFFFFFFF;
+    for (auto const& itr : bg->GetPlayers())
+    {
+        Player* enemy = ObjectAccessor::FindPlayer(itr.first);
+        if (!enemy || !enemy->IsAlive() || enemy->GetBgTeamId() == bot->GetBgTeamId())
+            continue;
+
+        uint32 score = (PlayerbotAI::IsHeal(enemy, true) ? 0 : 1000) + uint32(enemy->GetHealthPct()) / 15;
+        if (score < bestScore)
+        {
+            best = enemy;
+            bestScore = score;
+        }
+    }
+
+    return best;
+}
diff --git a/src/Ai/Base/Value/LeastHpTargetValue.h b/src/Ai/Base/Value/LeastHpTargetValue.h
index 5f7b2c74..fe441514 100644
--- a/src/Ai/Base/Value/LeastHpTargetValue.h
+++ b/src/Ai/Base/Value/LeastHpTargetValue.h
@@ -19,4 +19,15 @@ public:
     Unit* Calculate() override;
 };
 
+// Team-wide arena kill target: healers first, then lowest health. Computed
+// deterministically from shared world state so every team member independently
+// agrees on the same victim (no messaging needed).
+class ArenaKillTargetValue : public TargetValue
+{
+public:
+    ArenaKillTargetValue(PlayerbotAI* botAI, std::string const name = "arena kill target") : TargetValue(botAI, name) {}
+
+    Unit* Calculate() override;
+};
+
 #endif
diff --git a/src/Ai/Base/ValueContext.h b/src/Ai/Base/ValueContext.h
index 39262102..0f9983cb 100644
--- a/src/Ai/Base/ValueContext.h
+++ b/src/Ai/Base/ValueContext.h
@@ -259,6 +259,7 @@ public:
         creators["bg master"] = &ValueContext::bg_master;
         creators["enemy flag carrier"] = &ValueContext::enemy_fc;
         creators["team flag carrier"] = &ValueContext::team_fc;
+        creators["arena kill target"] = &ValueContext::arena_kill_target;
 
         creators["home bind"] = &ValueContext::home_bind;
         creators["last long move"] = &ValueContext::last_long_move;
@@ -511,6 +512,7 @@ private:
     static UntypedValue* rpg_bg_type(PlayerbotAI* botAI) { return new RpgBgTypeValue(botAI); }
     static UntypedValue* team_fc(PlayerbotAI* botAI) { return new FlagCarrierValue(botAI, true, true); }
     static UntypedValue* enemy_fc(PlayerbotAI* botAI) { return new FlagCarrierValue(botAI, false, true); }
+    static UntypedValue* arena_kill_target(PlayerbotAI* botAI) { return new ArenaKillTargetValue(botAI); }
 
     static UntypedValue* last_long_move(PlayerbotAI* botAI) { return new LastLongMoveValue(botAI); }
     static UntypedValue* home_bind(PlayerbotAI* botAI) { return new HomeBindValue(botAI); }
diff --git a/src/Ai/Base/WorldPacketActionContext.h b/src/Ai/Base/WorldPacketActionContext.h
index 9aa7eeaf..b4f2b4b2 100644
--- a/src/Ai/Base/WorldPacketActionContext.h
+++ b/src/Ai/Base/WorldPacketActionContext.h
@@ -103,6 +103,7 @@ public:
         creators["bg join"] = &WorldPacketActionContext::bg_join;
         creators["bg leave"] = &WorldPacketActionContext::bg_leave;
         creators["arena tactics"] = &WorldPacketActionContext::arena_tactics;
+        creators["arena burst sync"] = &WorldPacketActionContext::arena_burst_sync;
         creators["petition sign"] = &WorldPacketActionContext::petition_sign;
         creators["lfg join"] = &WorldPacketActionContext::lfg_join;
         creators["lfg accept"] = &WorldPacketActionContext::lfg_accept;
@@ -169,6 +170,7 @@ private:
     static Action* bg_status_check(PlayerbotAI* botAI) { return new BGStatusCheckAction(botAI); }
     static Action* bg_strategy_check(PlayerbotAI* botAI) { return new BGStrategyCheckAction(botAI); }
     static Action* arena_tactics(PlayerbotAI* botAI) { return new ArenaTactics(botAI); }
+    static Action* arena_burst_sync(PlayerbotAI* botAI) { return new ArenaBurstSyncAction(botAI); }
     static Action* petition_sign(PlayerbotAI* botAI) { return new PetitionSignAction(botAI); }
     static Action* lfg_teleport(PlayerbotAI* botAI) { return new LfgTeleportAction(botAI); }
     static Action* lfg_leave(PlayerbotAI* botAI) { return new LfgLeaveAction(botAI); }
diff --git a/src/Ai/World/Rpg/Action/NewRpgBaseAction.cpp b/src/Ai/World/Rpg/Action/NewRpgBaseAction.cpp
index e67a1c7c..8c8720f0 100644
--- a/src/Ai/World/Rpg/Action/NewRpgBaseAction.cpp
+++ b/src/Ai/World/Rpg/Action/NewRpgBaseAction.cpp
@@ -22,6 +22,7 @@
 #include "PlayerbotAIConfig.h"
 #include "PlayerbotTextMgr.h"
 #include "Playerbots.h"
+#include "DatabaseEnv.h"
 #include "Position.h"
 #include "QuestDef.h"
 #include "QuestPackets.h"
@@ -32,6 +33,8 @@
 #include "Timer.h"
 #include "TravelMgr.h"
 
+#include <mutex>
+
 bool NewRpgBaseAction::MoveFarTo(WorldPosition dest)
 {
     if (dest == WorldPosition())
@@ -1068,19 +1071,83 @@ bool NewRpgBaseAction::SelectRandomFlightTaxiNode(uint32& flightMasterEntry, Wor
     return true;
 }
 
+namespace
+{
+    // Resolves a bot's playstyle (RPG weight profile name) from its
+    // mod-ollama-chat personality via the shared characters DB. Cached per
+    // guid; empty results are retried every 5 min so bots that get their
+    // personality assigned after login still pick up a profile mid-session.
+    std::mutex playstyleCacheMutex;
+    std::unordered_map<ObjectGuid::LowType, std::pair<std::string, uint32>> playstyleCache;
+
+    std::string GetBotPlaystyle(Player* bot)
+    {
+        static int hasPlaystyleColumn = -1;
+        if (hasPlaystyleColumn == -1)
+        {
+            QueryResult probe = CharacterDatabase.Query(
+                "SELECT 1 FROM information_schema.COLUMNS WHERE TABLE_SCHEMA = DATABASE() "
+                "AND TABLE_NAME = 'mod_ollama_chat_personality_templates' AND COLUMN_NAME = 'playstyle'");
+            hasPlaystyleColumn = probe ? 1 : 0;
+            if (hasPlaystyleColumn)
+                LOG_INFO("playerbots", "[Playstyle] personality playstyle column found - per-personality RPG profiles active");
+        }
+        if (!hasPlaystyleColumn)
+            return "";
+
+        ObjectGuid::LowType guid = bot->GetGUID().GetCounter();
+        uint32 now = getMSTime();
+        {
+            std::lock_guard<std::mutex> lock(playstyleCacheMutex);
+            auto it = playstyleCache.find(guid);
+            if (it != playstyleCache.end() && (!it->second.first.empty() || now - it->second.second < 5 * MINUTE * IN_MILLISECONDS))
+                return it->second.first;
+        }
+
+        std::string playstyle;
+        QueryResult result = CharacterDatabase.Query(
+            "SELECT t.playstyle FROM mod_ollama_chat_personality p "
+            "JOIN mod_ollama_chat_personality_templates t ON t.`key` = p.personality "
+            "WHERE p.guid = {}", guid);
+        if (result)
+            playstyle = (*result)[0].Get<std::string>();
+        if (playstyle == "default")
+            playstyle.clear();
+        if (!playstyle.empty())
+            LOG_DEBUG("playerbots", "[Playstyle] Bot {} uses RPG profile '{}'", bot->GetName(), playstyle);
+
+        std::lock_guard<std::mutex> lock(playstyleCacheMutex);
+        playstyleCache[guid] = {playstyle, now};
+        return playstyle;
+    }
+}
+
 bool NewRpgBaseAction::RandomChangeStatus(std::vector<NewRpgStatus> candidateStatus)
 {
+    // Personality-driven playstyle profile if the bot has one, else the
+    // global weight table
+    std::string playstyle = GetBotPlaystyle(bot);
+    auto profileIt = sPlayerbotAIConfig.RpgStatusProbWeightProfiles.find(playstyle);
+    auto const& statusWeights = (!playstyle.empty() && profileIt != sPlayerbotAIConfig.RpgStatusProbWeightProfiles.end())
+        ? profileIt->second
+        : sPlayerbotAIConfig.RpgStatusProbWeight;
+    auto weightOf = [&statusWeights](NewRpgStatus status) -> uint32
+    {
+        auto it = statusWeights.find(status);
+        return it != statusWeights.end() ? it->second : 0;
+    };
+
     std::vector<NewRpgStatus> availableStatus;
     uint32 probSum = 0;
     for (NewRpgStatus status : candidateStatus)
     {
-        if (sPlayerbotAIConfig.RpgStatusProbWeight[status] == 0)
+        if (weightOf(status) == 0)
             continue;
 
         if (CheckRpgStatusAvailable(status))
         {
             availableStatus.push_back(status);
-            probSum += sPlayerbotAIConfig.RpgStatusProbWeight[status];
+            probSum += weightOf(status);
         }
     }
     // Safety check. Default to "rest" if all RPG weights = 0
@@ -1095,7 +1162,7 @@ bool NewRpgBaseAction::RandomChangeStatus(std::vector<NewRpgStatus> candidateSta
     NewRpgStatus chosenStatus = RPG_STATUS_END;
     for (NewRpgStatus status : availableStatus)
     {
-        accumulate += sPlayerbotAIConfig.RpgStatusProbWeight[status];
+        accumulate += weightOf(status);
         if (accumulate >= rand)
         {
             chosenStatus = status;
@@ -1103,6 +1170,9 @@ bool NewRpgBaseAction::RandomChangeStatus(std::vector<NewRpgStatus> candidateSta
         }
     }
 
+    LOG_DEBUG("playerbots", "[Playstyle] Bot {} (profile '{}') rolled status {}",
+              bot->GetName(), playstyle.empty() ? "default" : playstyle, static_cast<int>(chosenStatus));
+
     switch (chosenStatus)
     {
         case RPG_WANDER_RANDOM:
diff --git a/src/Ai/World/Rpg/Action/RpgSubActions.cpp b/src/Ai/World/Rpg/Action/RpgSubActions.cpp
index 24d03b7f..749cbd58 100644
--- a/src/Ai/World/Rpg/Action/RpgSubActions.cpp
+++ b/src/Ai/World/Rpg/Action/RpgSubActions.cpp
@@ -5,6 +5,12 @@
 
 #include "RpgSubActions.h"
 
+#include "AuctionHouseMgr.h"
+#include "Bag.h"
+#include "DatabaseEnv.h"
+#include "GameTime.h"
+#include "ItemTemplate.h"
+#include "ObjectMgr.h"
 #include "BudgetValues.h"
 #include "ChooseRpgTargetAction.h"
 #include "EmoteAction.h"
@@ -508,3 +514,129 @@ bool RpgMountAnimAction::Execute(Event /*event*/)
 
     return true;
 }
+
+// --- organic auction house selling -----------------------------------------
+
+// Personality-driven pricing factor applied to an item's base value. Sharp
+// traders and elitists overprice; clueless/generous types undervalue. Read
+// from the bot's mod-ollama-chat personality via the shared characters DB
+// (one query per listing pass, which is rare - gated behind being near an
+// auctioneer), 1.0 when unavailable. This is the *listing* number; the bot's
+// chat about the price is generated separately by the voice model.
+static float AhPriceFactorFor(Player* bot)
+{
+    static std::unordered_map<std::string, float> const factors = {
+        {"ELITE_ARENA_PVPER", 2.2f}, {"MIN_MAXER", 1.8f}, {"GOBLIN_MERCHANT", 1.7f},
+        {"TRADER", 1.6f}, {"GOLD_FARMER", 1.5f}, {"THEORYCRAFTER", 1.4f},
+        {"HARDCORE_RAIDLEAD", 1.3f}, {"FAIRWEATHER_FRIEND", 1.3f},
+        {"SCARED_NEWBIE", 0.5f}, {"ONE_WORD_GRUNTER", 0.5f}, {"FOOL", 0.4f},
+        {"STONER", 0.5f}, {"TWELVE_YEAR_OLD", 0.55f}, {"WOW_MOM", 0.7f},
+        {"HUMBLE_FARMER", 0.7f}, {"CHILL_DAD", 0.75f}, {"SILENT_TYPE", 0.7f},
+    };
+    std::string personality;
+    if (QueryResult r = CharacterDatabase.Query(
+            "SELECT personality FROM mod_ollama_chat_personality WHERE guid = {}", bot->GetGUID().GetCounter()))
+        personality = (*r)[0].Get<std::string>();
+    auto it = factors.find(personality);
+    return it != factors.end() ? it->second : 1.0f;
+}
+
+Creature* AhSellSparesAction::FindNearbyAuctioneer(Player* bot, PlayerbotAI* botAI)
+{
+    GuidVector npcs = botAI->GetAiObjectContext()->GetValue<GuidVector>("nearest npcs")->Get();
+    for (ObjectGuid const& guid : npcs)
+        if (Creature* auctioneer = bot->GetNPCIfCanInteractWith(guid, UNIT_NPC_FLAG_AUCTIONEER))
+            return auctioneer;
+    return nullptr;
+}
+
+bool AhSellSparesAction::Execute(Event /*event*/)
+{
+    Creature* auctioneer = FindNearbyAuctioneer(bot, botAI);
+    if (!auctioneer)
+        return false;
+
+    // respect the per-bot listing cap
+    uint32 active = 0;
+    if (QueryResult countResult = CharacterDatabase.Query(
+            "SELECT COUNT(*) FROM auctionhouse WHERE itemowner = {}", bot->GetGUID().GetCounter()))
+        active = (*countResult)[0].Get<uint32>();
+    if (active >= sPlayerbotAIConfig.botAhSellMaxListings)
+        return false;
+
+    AuctionHouseEntry const* ahEntry =
+        AuctionHouseMgr::GetAuctionHouseEntryFromFactionTemplate(auctioneer->GetFaction());
+    if (!ahEntry)
+        return false;
+    AuctionHouseObject* auctionHouse = sAuctionMgr->GetAuctionsMap(auctioneer->GetFaction());
+    if (!auctionHouse)
+        return false;
+
+    // collect a few sellable spares: tradeable, decent quality or trade goods
+    std::vector<Item*> spares;
+    auto consider = [&](Item* item)
+    {
+        if (!item || item->IsSoulBound() || spares.size() >= 3)
+            return;
+        ItemTemplate const* proto = item->GetTemplate();
+        bool sellable = (proto->Class == ITEM_CLASS_ARMOR || proto->Class == ITEM_CLASS_WEAPON)
+                            ? proto->Quality >= ITEM_QUALITY_UNCOMMON
+                            : (proto->Class == ITEM_CLASS_TRADE_GOODS && proto->Quality >= ITEM_QUALITY_NORMAL);
+        if (sellable && proto->SellPrice > 0)
+            spares.push_back(item);
+    };
+    for (uint8 i = INVENTORY_SLOT_ITEM_START; i < INVENTORY_SLOT_ITEM_END; ++i)
+        consider(bot->GetItemByPos(INVENTORY_SLOT_BAG_0, i));
+    for (uint8 b = INVENTORY_SLOT_BAG_START; b < INVENTORY_SLOT_BAG_END; ++b)
+        if (Bag* bag = bot->GetBagByPos(b))
+            for (uint32 s = 0; s < bag->GetBagSize(); ++s)
+                consider(bag->GetItemByPos(s));
+
+    if (spares.empty())
+        return false;
+
+    float priceFactor = AhPriceFactorFor(bot);
+    uint32 listed = 0;
+    for (Item* item : spares)
+    {
+        ItemTemplate const* proto = item->GetTemplate();
+        uint32 base = std::max<uint32>(proto->SellPrice, 25) * 3 * item->GetCount();
+        uint32 bid = std::max<uint32>(uint32(base * priceFactor) * urand(80, 120) / 100, 100);
+        uint32 buyout = bid * 13 / 10;
+
+        AuctionEntry* auction = new AuctionEntry();
+        auction->Id = sObjectMgr->GenerateAuctionID();
+        auction->houseId = AuctionHouseId(ahEntry->houseId);
+        auction->item_guid = item->GetGUID();
+        auction->item_template = item->GetEntry();
+        auction->itemCount = item->GetCount();
+        auction->owner = bot->GetGUID();
+        auction->startbid = bid;
+        auction->bidder = ObjectGuid::Empty;
+        auction->bid = 0;
+        auction->buyout = buyout;
+        auction->expire_time = GameTime::GetGameTime().count() +
+                               uint32(12 * HOUR * sWorld->getRate(RATE_AUCTION_TIME));
+        auction->deposit = 0;
+        auction->auctionHouseEntry = ahEntry;
+
+        sAuctionMgr->AddAItem(item);
+        auctionHouse->AddAuction(auction);
+        bot->MoveItemFromInventory(item->GetBagSlot(), item->GetSlot(), true);
+
+        CharacterDatabaseTransaction trans = CharacterDatabase.BeginTransaction();
+        item->DeleteFromInventoryDB(trans);
+        item->SaveToDB(trans);
+        auction->SaveToDB(trans);
+        bot->SaveInventoryAndGoldToDB(trans);
+        CharacterDatabase.CommitTransaction(trans);
+        ++listed;
+
+        LOG_DEBUG("playerbots", "[BotAH] {} listed {} x{} (bid {} buyout {} copper)",
+                  bot->GetName(), proto->Name1, item->GetCount(), bid, buyout);
+    }
+
+    if (listed)
+        LOG_INFO("playerbots", "[BotAH] {} listed {} item(s) at the auction house", bot->GetName(), listed);
+    return listed > 0;
+}
diff --git a/src/Ai/World/Rpg/Action/RpgSubActions.h b/src/Ai/World/Rpg/Action/RpgSubActions.h
index 95bcac6e..c9f3e6ea 100644
--- a/src/Ai/World/Rpg/Action/RpgSubActions.h
+++ b/src/Ai/World/Rpg/Action/RpgSubActions.h
@@ -160,6 +160,19 @@ private:
     std::string const ActionName() override;
 };
 
+// Organic bot economy: when a random bot ends up near an auctioneer (towns,
+// via WanderNpc), it lists a few of its real spare items on its faction's
+// auction house at sane prices. See AhSellSparesTrigger for the odds.
+class AhSellSparesAction : public Action
+{
+public:
+    AhSellSparesAction(PlayerbotAI* botAI) : Action(botAI, "ah sell spares") {}
+
+    bool Execute(Event event) override;
+
+    static Creature* FindNearbyAuctioneer(Player* bot, PlayerbotAI* botAI);
+};
+
 class RpgTrainAction : public RpgSubAction
 {
 public:
diff --git a/src/Ai/World/Rpg/Strategy/NewRpgStrategy.cpp b/src/Ai/World/Rpg/Strategy/NewRpgStrategy.cpp
index 521b15c3..260ed1e3 100644
--- a/src/Ai/World/Rpg/Strategy/NewRpgStrategy.cpp
+++ b/src/Ai/World/Rpg/Strategy/NewRpgStrategy.cpp
@@ -73,6 +73,24 @@ void NewRpgStrategy::InitTriggers(std::vector<TriggerNode*>& triggers)
             }
         )
     );
+    // sentiment-gated proactive quest-help invites toward nearby real players
+    triggers.push_back(
+        new TriggerNode(
+            "quest help offer",
+            {
+                NextAction("offer quest help", 12.0f)
+            }
+        )
+    );
+    // organic economy: list spare items when near an auctioneer
+    triggers.push_back(
+        new TriggerNode(
+            "ah sell spares",
+            {
+                NextAction("ah sell spares", 12.0f)
+            }
+        )
+    );
 }
 
 void NewRpgStrategy::InitMultipliers(std::vector<Multiplier*>&)
diff --git a/src/Ai/World/Rpg/Trigger/NewRpgTrigger.cpp b/src/Ai/World/Rpg/Trigger/NewRpgTrigger.cpp
index 65e78eb4..3d916de6 100644
--- a/src/Ai/World/Rpg/Trigger/NewRpgTrigger.cpp
+++ b/src/Ai/World/Rpg/Trigger/NewRpgTrigger.cpp
@@ -1,4 +1,29 @@
 #include "NewRpgTriggers.h"
+#include "InviteToGroupAction.h"
 #include "PlayerbotAI.h"
+#include "PlayerbotAIConfig.h"
+#include "Random.h"
+#include "RpgSubActions.h"
 
 bool NewRpgStatusTrigger::IsActive() { return status == botAI->rpgInfo.GetStatus(); }
+
+bool QuestHelpOfferTrigger::IsActive()
+{
+    int tier = 0;
+    if (!OfferQuestHelpAction::FindQuestHelpTarget(bot, botAI, tier))
+        return false;
+
+    float chance = tier == 2 ? sPlayerbotAIConfig.questHelpConfirmedChance
+                 : tier == 1 ? sPlayerbotAIConfig.questHelpNearbyChance
+                             : sPlayerbotAIConfig.questHelpRandomChance;
+    return roll_chance_f(chance);
+}
+
+bool AhSellSparesTrigger::IsActive()
+{
+    if (sPlayerbotAIConfig.botAhSellChance <= 0.0f || bot->GetGroup() || bot->IsInCombat())
+        return false;
+    if (!AhSellSparesAction::FindNearbyAuctioneer(bot, botAI))
+        return false;
+    return roll_chance_f(sPlayerbotAIConfig.botAhSellChance);
+}
diff --git a/src/Ai/World/Rpg/Trigger/NewRpgTriggers.h b/src/Ai/World/Rpg/Trigger/NewRpgTriggers.h
index 15b2243d..37c701df 100644
--- a/src/Ai/World/Rpg/Trigger/NewRpgTriggers.h
+++ b/src/Ai/World/Rpg/Trigger/NewRpgTriggers.h
@@ -17,4 +17,24 @@ protected:
     NewRpgStatus status;
 };
 
+// Fires (rarely) when an ungrouped bot in good sentiment standing with a
+// nearby real player decides to offer quest help - most likely when it can
+// confirm the player shares its active quest.
+class QuestHelpOfferTrigger : public Trigger
+{
+public:
+    QuestHelpOfferTrigger(PlayerbotAI* botAI) : Trigger(botAI, "quest help offer", 30) {}
+
+    bool IsActive() override;
+};
+
+// Fires when a bot with spare sellable items is near an auctioneer.
+class AhSellSparesTrigger : public Trigger
+{
+public:
+    AhSellSparesTrigger(PlayerbotAI* botAI) : Trigger(botAI, "ah sell spares", 30) {}
+
+    bool IsActive() override;
+};
+
 #endif
diff --git a/src/Bot/Factory/AiFactory.cpp b/src/Bot/Factory/AiFactory.cpp
index dfbe1f96..0750915c 100644
--- a/src/Bot/Factory/AiFactory.cpp
+++ b/src/Bot/Factory/AiFactory.cpp
@@ -482,7 +482,9 @@ void AiFactory::AddDefaultCombatStrategies(Player* player, PlayerbotAI* const fa
         if (player->InArena())
         {
             engine->addStrategy("arena", false);
-            engine->addStrategiesNoInit("boost", "racials", "chat", "default", "aoe", "cast time", "dps assist", nullptr);
+            // no "boost" here: burst cooldowns are held until the team burst
+            // window opens (see ArenaBurstWindowTrigger / arena burst sync)
+            engine->addStrategiesNoInit("racials", "chat", "default", "aoe", "cast time", "dps assist", nullptr);
         }
         else
             engine->addStrategiesNoInit("boost", "racials", "chat", "default", "aoe", "potions", "cast time", "dps assist", nullptr);
diff --git a/src/Bot/Factory/GuildRecruitmentEvent.cpp b/src/Bot/Factory/GuildRecruitmentEvent.cpp
new file mode 100644
index 00000000..8ca23285
--- /dev/null
+++ b/src/Bot/Factory/GuildRecruitmentEvent.cpp
@@ -0,0 +1,324 @@
+/*
+ * Copyright (C) 2016+ AzerothCore <www.azerothcore.org>, released under GNU AGPL v3 license, you may redistribute it
+ * and/or modify it under version 3 of the License, or (at your option), any later version.
+ */
+
+#include "GuildRecruitmentEvent.h"
+
+#include "Cell.h"
+#include "CellImpl.h"
+#include "Common.h"
+#include "Config.h"
+#include "DatabaseEnv.h"
+#include "GameTime.h"
+#include "GridNotifiers.h"
+#include "GridNotifiersImpl.h"
+#include "Guild.h"
+#include "Log.h"
+#include "ObjectAccessor.h"
+#include "Opcodes.h"
+#include "Player.h"
+#include "PlayerbotMgr.h"
+#include "Random.h"
+#include "StringFormat.h"
+#include "WorldPacket.h"
+#include "WorldSession.h"
+
+#include <list>
+#include <unordered_map>
+
+// Provided by mod-ollama-chat (same binary): make a bot SAY or WHISPER a short,
+// in-character, LLM-generated line about a concrete situation. Weakly optional -
+// if the chat module is disabled the symbol is null and the call is a no-op.
+// Sibling of PersonalityGuildFactory's OllamaChat_RenameGuildInVoice weak hook.
+[[gnu::weak]] void OllamaChat_SpeakSituation(Player* bot, Player* target, std::string const& situation, bool whisper);
+
+namespace
+{
+    // Heavy work (grid scans, pitches) is throttled to this cadence; the driver
+    // is called every world tick but only acts every ~5s.
+    constexpr uint32 kTickIntervalMs = 5 * IN_MILLISECONDS;
+    // Realm-start gate: skip recruitment when the server has been up longer.
+    constexpr uint32 kRealmStartUptimeSec = 30 * MINUTE;
+    // Hard cap on invites a single leader sends over the whole event.
+    constexpr uint32 kMaxInvitesPerLeader = 10;
+
+    void SpeakSituation(Player* bot, Player* target, std::string const& situation, bool whisper)
+    {
+        if (OllamaChat_SpeakSituation)
+            OllamaChat_SpeakSituation(bot, target, situation, whisper);
+    }
+
+    struct StartPos
+    {
+        uint32 mapId = 0;
+        float x = 0.0f;
+        float y = 0.0f;
+        float z = 0.0f;
+        float o = 0.0f;
+        TeamId team = TEAM_ALLIANCE;
+    };
+
+    // One starting spawn per race (Northshire, Coldridge, Valley of Trials, ...).
+    // Classes of the same race share a spawn, so the first row per race wins.
+    std::unordered_map<uint8, StartPos> LoadStartPositions()
+    {
+        std::unordered_map<uint8, StartPos> out;
+        QueryResult result = WorldDatabase.Query(
+            "SELECT race, map, position_x, position_y, position_z, orientation FROM playercreateinfo");
+        if (!result)
+            return out;
+
+        do
+        {
+            Field* fields = result->Fetch();
+            uint8 race = fields[0].Get<uint8>();
+            if (out.count(race))
+                continue;
+
+            StartPos pos;
+            pos.mapId = fields[1].Get<uint32>();
+            pos.x = fields[2].Get<float>();
+            pos.y = fields[3].Get<float>();
+            pos.z = fields[4].Get<float>();
+            pos.o = fields[5].Get<float>();
+            pos.team = Player::TeamIdForRace(race);
+            out[race] = pos;
+        } while (result->NextRow());
+
+        return out;
+    }
+
+    // Resolve the starting spawn for a leader: its own race first, else any
+    // same-faction race (defensive - every playable race has a createinfo row).
+    StartPos const* ResolveStart(std::unordered_map<uint8, StartPos> const& starts, Player* leader)
+    {
+        auto it = starts.find(leader->getRace());
+        if (it != starts.end())
+            return &it->second;
+
+        TeamId const team = leader->GetTeamId();
+        for (auto const& kv : starts)
+            if (kv.second.team == team)
+                return &kv.second;
+
+        return nullptr;
+    }
+
+    // Queue a CMSG_GUILD_INVITE on the leader's session for `target`, exactly as
+    // the real client would - mirrors the module's own GuildInviteAction (payload
+    // is just the invitee name). The session dispatches it to
+    // HandleGuildInviteOpcode on its next update, running the full core path
+    // (faction / already-guilded / already-invited / rank checks) and popping the
+    // SMSG_GUILD_INVITE dialog on the target. QueuePacket takes ownership.
+    void SendGuildInvite(Player* leader, Player* target)
+    {
+        WorldSession* session = leader->GetSession();
+        if (!session)
+            return;
+
+        WorldPacket* data = new WorldPacket(CMSG_GUILD_INVITE);
+        *data << target->GetName();
+        session->QueuePacket(data);
+    }
+
+    // First nearby REAL player that is a fresh, unguilded, same-faction recruit
+    // this leader has not already invited. Returns nullptr when none qualify.
+    Player* FindNearbyRecruit(Player* leader, float range, std::unordered_set<ObjectGuid> const& alreadyInvited)
+    {
+        std::list<Player*> nearby;
+        Acore::AnyPlayerInObjectRangeCheck check(leader, range);
+        Acore::PlayerListSearcher<Acore::AnyPlayerInObjectRangeCheck> searcher(leader, nearby, check);
+        Cell::VisitObjects(leader, searcher, range);
+
+        TeamId const team = leader->GetTeamId();
+        for (Player* target : nearby)
+        {
+            if (!target || target == leader)
+                continue;
+            if (sPlayerbotsMgr.GetPlayerbotAI(target))  // skip bots - invite REAL players only
+                continue;
+            if (target->GetTeamId() != team)  // guild invites are same-faction
+                continue;
+            if (target->GetLevel() >= 10)  // brand-new players only
+                continue;
+            if (target->GetGuildId() || target->GetGuildIdInvited())
+                continue;  // already guilded, or already has a pending invite
+            if (alreadyInvited.count(target->GetGUID()))
+                continue;  // one invite per player per leader
+
+            return target;
+        }
+
+        return nullptr;
+    }
+}  // namespace
+
+GuildRecruitmentEvent& GuildRecruitmentEvent::instance()
+{
+    static GuildRecruitmentEvent instance;
+    return instance;
+}
+
+void GuildRecruitmentEvent::Begin(std::vector<std::pair<ObjectGuid, std::string>> const& leaders)
+{
+    if (!sConfigMgr->GetOption<bool>("AiPlayerbot.GuildRecruitEnabled", true))
+        return;
+
+    // At least one guild must have formed this process (non-empty leaders), and
+    // it must be a realm start - not a long-running server that happened to form
+    // a guild much later.
+    if (leaders.empty())
+        return;
+
+    if (uint32(GameTime::GetUptime().count()) >= kRealmStartUptimeSec)
+    {
+        LOG_INFO("playerbots", "[GuildRecruit] Skipping recruitment - server uptime exceeds {} min",
+                 kRealmStartUptimeSec / MINUTE);
+        return;
+    }
+
+    uint32 const minutes = sConfigMgr->GetOption<uint32>("AiPlayerbot.GuildRecruitMinutes", 15);
+
+    std::unordered_map<uint8, StartPos> const starts = LoadStartPositions();
+    if (starts.empty())
+    {
+        LOG_WARN("playerbots", "[GuildRecruit] No playercreateinfo start positions found; recruitment disabled");
+        return;
+    }
+
+    for (auto const& entry : leaders)
+    {
+        Player* leader = ObjectAccessor::FindPlayer(entry.first);
+        if (!leader)
+            continue;
+
+        StartPos const* start = ResolveStart(starts, leader);
+        if (!start)
+            continue;
+
+        Recruiter recruiter;
+        recruiter.leaderGuid = entry.first;
+        recruiter.archetype = entry.second;
+        recruiter.guildId = leader->GetGuildId();
+        if (Guild* guild = leader->GetGuild())
+            recruiter.guildName = guild->GetName();
+        recruiter.returnMapId = leader->GetMapId();
+        recruiter.returnX = leader->GetPositionX();
+        recruiter.returnY = leader->GetPositionY();
+        recruiter.returnZ = leader->GetPositionZ();
+        recruiter.returnO = leader->GetOrientation();
+        recruiter.remainingMs = minutes * MINUTE * IN_MILLISECONDS;
+        recruiter.nextPitchMs = urand(10, 20) * IN_MILLISECONDS;  // first pitch shortly after arrival
+
+        leader->TeleportTo(start->mapId, start->x, start->y, start->z, start->o);
+
+        LOG_INFO("playerbots", "[GuildRecruit] {} left to recruit for {} guild '{}' at its starting area",
+                 leader->GetName(), recruiter.archetype, recruiter.guildName);
+
+        _recruiters.push_back(std::move(recruiter));
+    }
+
+    _tickAccumMs = 0;
+}
+
+void GuildRecruitmentEvent::Update(uint32 diff)
+{
+    if (_recruiters.empty())
+        return;
+
+    _tickAccumMs += diff;
+    if (_tickAccumMs < kTickIntervalMs)
+        return;
+
+    uint32 const elapsed = _tickAccumMs;
+    _tickAccumMs = 0;
+    Tick(elapsed);
+}
+
+void GuildRecruitmentEvent::Tick(uint32 elapsed)
+{
+    float const range = sConfigMgr->GetOption<float>("AiPlayerbot.GuildRecruitInviteRange", 20.0f);
+
+    for (size_t i = 0; i < _recruiters.size();)
+    {
+        Recruiter& recruiter = _recruiters[i];
+
+        Player* leader = ObjectAccessor::FindPlayer(recruiter.leaderGuid);
+        if (!leader)
+        {
+            // Leader logged out mid-event: drop it, nothing to teleport back.
+            LOG_INFO("playerbots", "[GuildRecruit] Leader for '{}' went offline; ending recruitment ({} invites sent)",
+                     recruiter.guildName, recruiter.invitesSent);
+            _recruiters.erase(_recruiters.begin() + i);
+            continue;
+        }
+
+        // Window elapsed -> teleport home and finish.
+        if (recruiter.remainingMs <= elapsed)
+        {
+            Finish(recruiter, true);
+            _recruiters.erase(_recruiters.begin() + i);
+            continue;
+        }
+        recruiter.remainingMs -= elapsed;
+
+        // Wait until the cross-map teleport has landed before pitching or scanning -
+        // mid-teleport the leader has no valid grid neighbours.
+        if (leader->IsBeingTeleported() || !leader->IsInWorld())
+        {
+            ++i;
+            continue;
+        }
+
+        // Periodic in-voice pitch (every 60-90s, jittered per leader).
+        if (recruiter.nextPitchMs <= elapsed)
+        {
+            recruiter.nextPitchMs = urand(60, 90) * IN_MILLISECONDS;
+            SpeakSituation(leader, nullptr,
+                           Acore::StringFormat(
+                               "you're recruiting new members for your {} guild '{}' at the starting area - "
+                               "make a short friendly recruitment pitch to the new players around",
+                               recruiter.archetype, recruiter.guildName),
+                           false);
+            LOG_DEBUG("playerbots", "[GuildRecruit] {} pitched '{}'", leader->GetName(), recruiter.guildName);
+        }
+        else
+        {
+            recruiter.nextPitchMs -= elapsed;
+        }
+
+        // Invite one nearby fresh real player per tick (spreads invites naturally
+        // as newcomers wander in, and avoids a burst of whispers).
+        if (recruiter.invitesSent < kMaxInvitesPerLeader)
+        {
+            if (Player* target = FindNearbyRecruit(leader, range, recruiter.invited))
+            {
+                SendGuildInvite(leader, target);
+                recruiter.invited.insert(target->GetGUID());
+                ++recruiter.invitesSent;
+                LOG_INFO("playerbots", "[GuildRecruit] {} invited {} to '{}' ({}/{})", leader->GetName(),
+                         target->GetName(), recruiter.guildName, recruiter.invitesSent, kMaxInvitesPerLeader);
+                SpeakSituation(
+                    leader, target,
+                    Acore::StringFormat("you just sent {} a guild invite - tell them what your guild is about, briefly",
+                                        target->GetName()),
+                    true);
+            }
+        }
+
+        ++i;
+    }
+}
+
+void GuildRecruitmentEvent::Finish(Recruiter const& recruiter, bool teleportBack)
+{
+    Player* leader = ObjectAccessor::FindPlayer(recruiter.leaderGuid);
+    if (leader && teleportBack)
+        leader->TeleportTo(recruiter.returnMapId, recruiter.returnX, recruiter.returnY, recruiter.returnZ,
+                           recruiter.returnO);
+
+    std::string const who = leader ? leader->GetName() : recruiter.leaderGuid.ToString();
+    LOG_INFO("playerbots", "[GuildRecruit] {} finished recruiting for '{}' ({} invites sent)", who, recruiter.guildName,
+             recruiter.invitesSent);
+}
diff --git a/src/Bot/Factory/GuildRecruitmentEvent.h b/src/Bot/Factory/GuildRecruitmentEvent.h
new file mode 100644
index 00000000..d8c46e77
--- /dev/null
+++ b/src/Bot/Factory/GuildRecruitmentEvent.h
@@ -0,0 +1,69 @@
+/*
+ * Copyright (C) 2016+ AzerothCore <www.azerothcore.org>, released under GNU AGPL v3 license, you may redistribute it
+ * and/or modify it under version 3 of the License, or (at your option), any later version.
+ */
+
+#ifndef PLAYERBOTS_GUILDRECRUITMENTEVENT_H
+#define PLAYERBOTS_GUILDRECRUITMENTEVENT_H
+
+#include "ObjectGuid.h"
+
+#include <string>
+#include <unordered_set>
+#include <utility>
+#include <vector>
+
+// Realm-start guild recruitment. Right after personality guilds form on a fresh
+// world, each guild LEADER travels to its faction's starting area and spends
+// ~15 minutes recruiting: it periodically pitches its guild in its own voice and
+// sends guild invites to nearby unguilded, low-level REAL players (the invite
+// popup is the ask - very 2008). When the window closes each leader teleports
+// back to where it was and resumes normal bot life. Driven from a world-thread
+// tick; cheap when idle. Guarded so it only fires as a realm-start experience.
+class GuildRecruitmentEvent
+{
+public:
+    static GuildRecruitmentEvent& instance();
+
+    // Called at the end of PersonalityGuildFactory::FormPersonalityGuilds with
+    // the freshly-formed guild leaders (leader guid + archetype name). Teleports
+    // each leader to its race's starting area and opens a recruiting window.
+    // No-op when disabled, when leaders is empty, or when the server has been up
+    // too long (not a realm start).
+    void Begin(std::vector<std::pair<ObjectGuid, std::string>> const& leaders);
+
+    // World-thread tick driver; call every world update. Returns immediately when
+    // no recruitment is in progress. Heavy work is throttled to every ~5s.
+    void Update(uint32 diff);
+
+private:
+    GuildRecruitmentEvent() = default;
+
+    struct Recruiter
+    {
+        ObjectGuid leaderGuid;
+        std::string archetype;
+        std::string guildName;
+        uint32 guildId = 0;
+
+        // Where the leader was before it left to recruit - teleported back after.
+        uint32 returnMapId = 0;
+        float returnX = 0.0f;
+        float returnY = 0.0f;
+        float returnZ = 0.0f;
+        float returnO = 0.0f;
+
+        uint32 remainingMs = 0;   // recruiting window left
+        uint32 nextPitchMs = 0;   // countdown to the next in-voice pitch
+        uint32 invitesSent = 0;   // total invites this event (capped)
+        std::unordered_set<ObjectGuid> invited;  // players already invited by this leader
+    };
+
+    void Tick(uint32 elapsed);
+    void Finish(Recruiter const& recruiter, bool teleportBack);
+
+    std::vector<Recruiter> _recruiters;
+    uint32 _tickAccumMs = 0;
+};
+
+#endif
diff --git a/src/Bot/Factory/OngoingGuildRecruit.cpp b/src/Bot/Factory/OngoingGuildRecruit.cpp
new file mode 100644
index 00000000..80aac54e
--- /dev/null
+++ b/src/Bot/Factory/OngoingGuildRecruit.cpp
@@ -0,0 +1,352 @@
+/*
+ * Copyright (C) 2016+ AzerothCore <www.azerothcore.org>, released under GNU AGPL v3 license, you may redistribute it
+ * and/or modify it under version 3 of the License, or (at your option), any later version.
+ */
+
+#include "OngoingGuildRecruit.h"
+
+#include "Cell.h"
+#include "CellImpl.h"
+#include "Common.h"
+#include "Config.h"
+#include "DatabaseEnv.h"
+#include "GridNotifiers.h"
+#include "GridNotifiersImpl.h"
+#include "Guild.h"
+#include "GuildMgr.h"
+#include "Log.h"
+#include "ObjectAccessor.h"
+#include "Opcodes.h"
+#include "Player.h"
+#include "PlayerbotAIConfig.h"
+#include "PlayerbotMgr.h"
+#include "Random.h"
+#include "RandomPlayerbotMgr.h"
+#include "StringFormat.h"
+#include "Timer.h"
+#include "WorldPacket.h"
+#include "WorldSession.h"
+
+#include <algorithm>
+#include <list>
+#include <map>
+#include <string>
+#include <unordered_map>
+#include <unordered_set>
+
+// Provided by mod-ollama-chat (same binary): make a bot SAY or WHISPER a short,
+// in-character, LLM-generated line about a concrete situation. Weakly optional -
+// null (and the call a no-op) when the chat module is disabled. Same weak hook
+// GuildRecruitmentEvent uses for its realm-start pitches.
+[[gnu::weak]] void OllamaChat_SpeakSituation(Player* bot, Player* target, std::string const& situation, bool whisper);
+
+namespace
+{
+    // Global "last time the REAL player was sent a guild invite" (any leader).
+    // World-thread-only access (the recruiter tick), so a plain static is safe.
+    uint32 g_lastPlayerInviteMs = 0;
+
+    void SpeakSituation(Player* bot, Player* target, std::string const& situation, bool whisper)
+    {
+        if (OllamaChat_SpeakSituation)
+            OllamaChat_SpeakSituation(bot, target, situation, whisper);
+    }
+
+    using PersonalitySet = std::unordered_set<std::string>;
+
+    // Authoritative leader / fit sets - a verbatim mirror of PersonalityGuildFactory's
+    // (they live in that file's anonymous namespace, so cannot be shared directly).
+    // Archetype and elite fit enforcement below reconstruct a guild's original rules
+    // from its leader's personality, keeping recruitment consistent with formation.
+    PersonalitySet const RAID_LEADERS   = {"HARDCORE_RAIDLEAD", "THEORYCRAFTER", "MIN_MAXER", "LORE_NERD"};
+    PersonalitySet const PVP_LEADERS    = {"ELITE_ARENA_PVPER", "PVP_TRASHTALKER", "DUELIST", "RAGER"};
+    PersonalitySet const CASUAL_LEADERS = {"WOW_MOM",     "MENTOR",           "GUILD_RECRUITER",
+                                           "CHILL_DAD",   "JOLLY_BEER_LOVER", "HEROIC_LEADER"};
+
+    PersonalitySet const RAID_FIT = {"RAIDER",      "THEORYCRAFTER", "MIN_MAXER",          "HARDCORE_RAIDLEAD",
+                                     "HEALER_MAIN", "SPEEDRUNNER",   "ACHIEVEMENT_HUNTER", "LORE_NERD"};
+    PersonalitySet const PVP_FIT  = {"PVP_HARDCORE", "PVP_TRASHTALKER", "DUELIST",
+                                     "ELITE_ARENA_PVPER", "RAGER", "EDGE_LORD"};
+
+    // How often (0-100) a guild leader with this personality opportunistically
+    // recruits during normal play - the "frequently a party leader / interactive"
+    // gate. Any other recognised leader personality gets 25; a non-leader
+    // personality gets 0 and never recruits (guild leaders only reach here anyway).
+    float OngoingRecruitChanceFor(std::string const& personality)
+    {
+        // Tight per-check chances (combined with a 3-min interval + 2-hr per-pair
+        // cooldown, so even the chattiest recruiter invites a given target rarely).
+        static std::unordered_map<std::string, float> const kChances = {
+            {"GUILD_RECRUITER", 50.0f}, {"LFG_SPAMMER", 35.0f},       {"HEROIC_LEADER", 30.0f},
+            {"MENTOR", 28.0f},          {"WOW_MOM", 28.0f},           {"JOLLY_BEER_LOVER", 22.0f},
+            {"CHILL_DAD", 22.0f},       {"HARDCORE_RAIDLEAD", 20.0f}, {"ELITE_ARENA_PVPER", 15.0f},
+        };
+        auto it = kChances.find(personality);
+        if (it != kChances.end())
+            return it->second;
+        if (RAID_LEADERS.count(personality) || PVP_LEADERS.count(personality) || CASUAL_LEADERS.count(personality))
+            return 12.0f;
+        return 0.0f;
+    }
+
+    // The fit-set an elite guild enforces on bot recruits, derived from its
+    // leader's personality; nullptr for a casual guild (anyone fits).
+    PersonalitySet const* FitSetForLeader(std::string const& leaderPersonality)
+    {
+        if (RAID_LEADERS.count(leaderPersonality))
+            return &RAID_FIT;
+        if (PVP_LEADERS.count(leaderPersonality))
+            return &PVP_FIT;
+        return nullptr;  // casual: no fit requirement
+    }
+
+    char const* ArchetypeName(std::string const& leaderPersonality)
+    {
+        if (RAID_LEADERS.count(leaderPersonality))
+            return "raid";
+        if (PVP_LEADERS.count(leaderPersonality))
+            return "pvp";
+        return "casual";
+    }
+
+    // guid -> personality from the shared characters DB, guid-cached. Empty
+    // results are re-checked after 5 min; a found personality is cached for good.
+    // Mirrors GetBotPlaystyle's cache shape (NewRpgBaseAction.cpp).
+    std::string GetPersonality(ObjectGuid guid)
+    {
+        static int hasTable = -1;
+        if (hasTable == -1)
+            hasTable = CharacterDatabase.Query(
+                          "SELECT 1 FROM information_schema.TABLES WHERE TABLE_SCHEMA = DATABASE() "
+                          "AND TABLE_NAME = 'mod_ollama_chat_personality'")
+                          ? 1
+                          : 0;
+        if (!hasTable)
+            return "";
+
+        ObjectGuid::LowType const low = guid.GetCounter();
+        uint32 const now = getMSTime();
+        static std::mutex cacheMutex;
+        static std::map<ObjectGuid::LowType, std::pair<std::string, uint32>> cache;
+        {
+            std::lock_guard<std::mutex> lock(cacheMutex);
+            auto it = cache.find(low);
+            if (it != cache.end() &&
+                (!it->second.first.empty() || now - it->second.second < 5 * MINUTE * IN_MILLISECONDS))
+                return it->second.first;
+        }
+
+        std::string personality;
+        if (QueryResult result = CharacterDatabase.Query(
+                "SELECT personality FROM mod_ollama_chat_personality WHERE guid = {}", low))
+            personality = (*result)[0].Get<std::string>();
+
+        std::lock_guard<std::mutex> lock(cacheMutex);
+        cache[low] = {personality, now};
+        return personality;
+    }
+
+    // Recruiter -> real-player sentiment (shared characters DB, guid counters),
+    // cached per pair; neutral 0.5 when the chat module or the row is absent.
+    // Mirrors InviteToGroupAction's GetBotPlayerSentiment.
+    float GetSentiment(Player* bot, Player* player)
+    {
+        static int hasTable = -1;
+        if (hasTable == -1)
+            hasTable = CharacterDatabase.Query(
+                          "SELECT 1 FROM information_schema.TABLES WHERE TABLE_SCHEMA = DATABASE() "
+                          "AND TABLE_NAME = 'mod_ollama_chat_bot_player_sentiments'")
+                          ? 1
+                          : 0;
+        if (!hasTable)
+            return 0.5f;
+
+        static std::mutex cacheMutex;
+        static std::map<std::pair<ObjectGuid::LowType, ObjectGuid::LowType>, std::pair<float, uint32>> cache;
+        auto const key = std::make_pair(bot->GetGUID().GetCounter(), player->GetGUID().GetCounter());
+        uint32 const now = getMSTime();
+        {
+            std::lock_guard<std::mutex> lock(cacheMutex);
+            auto it = cache.find(key);
+            if (it != cache.end() && now - it->second.second < 5 * MINUTE * IN_MILLISECONDS)
+                return it->second.first;
+        }
+
+        float value = 0.5f;
+        if (QueryResult result = CharacterDatabase.Query(
+                "SELECT sentiment_value FROM mod_ollama_chat_bot_player_sentiments "
+                "WHERE bot_guid = {} AND player_guid = {}",
+                key.first, key.second))
+            value = (*result)[0].Get<float>();
+
+        std::lock_guard<std::mutex> lock(cacheMutex);
+        cache[key] = {value, now};
+        return value;
+    }
+
+    // Queue a CMSG_GUILD_INVITE on the leader's session for `target` - identical
+    // to GuildRecruitmentEvent::SendGuildInvite. The core runs the full invite
+    // path; the target pops SMSG_GUILD_INVITE. For a BOT target that outgoing
+    // packet is caught by its own "guild invite" handler (PlayerbotAI.cpp) which
+    // the world_packet ("default") strategy routes to GuildAcceptAction, so the
+    // bot auto-accepts. A real player gets the dialog and chooses.
+    void SendGuildInvite(Player* leader, Player* target)
+    {
+        WorldSession* session = leader->GetSession();
+        if (!session)
+            return;
+
+        WorldPacket* data = new WorldPacket(CMSG_GUILD_INVITE);
+        *data << target->GetName();
+        session->QueuePacket(data);
+    }
+}  // namespace
+
+OngoingGuildRecruit& OngoingGuildRecruit::instance()
+{
+    static OngoingGuildRecruit instance;
+    return instance;
+}
+
+void OngoingGuildRecruit::Update(uint32 diff)
+{
+    if (!sConfigMgr->GetOption<bool>("AiPlayerbot.OngoingRecruitEnabled", true))
+        return;
+
+    _accumMs += diff;
+    uint32 const intervalMs =
+        sConfigMgr->GetOption<uint32>("AiPlayerbot.OngoingRecruitIntervalSec", 180) * IN_MILLISECONDS;
+    if (_accumMs < intervalMs)
+        return;
+
+    _accumMs = 0;
+    Tick();
+}
+
+void OngoingGuildRecruit::Tick()
+{
+    float const range = sConfigMgr->GetOption<float>("AiPlayerbot.OngoingRecruitRange", 30.0f);
+    uint32 const cooldownMin = sConfigMgr->GetOption<uint32>("AiPlayerbot.OngoingRecruitCooldownMin", 120);
+    float const sentimentBonus = sConfigMgr->GetOption<float>("AiPlayerbot.OngoingRecruitSentimentBonus", 15.0f);
+    // Global anti-spam for the REAL player: at most one guild invite popup per
+    // this window across ALL leaders, no matter how many they pass.
+    uint32 const playerCdMs =
+        sConfigMgr->GetOption<uint32>("AiPlayerbot.OngoingRecruitPlayerCooldownMin", 30) * MINUTE * IN_MILLISECONDS;
+    uint32 const cooldownMs = cooldownMin * MINUTE * IN_MILLISECONDS;
+    uint32 const cap = std::max<uint32>(2, sPlayerbotAIConfig.randomBotGuildSizeMax);
+    uint32 const now = getMSTime();
+
+    for (auto const& kv : sRandomPlayerbotMgr.GetAllBots())
+    {
+        Player* leader = kv.second;
+        if (!leader || !leader->IsInWorld() || leader->IsBeingTeleported())
+            continue;
+
+        uint32 const guildId = leader->GetGuildId();
+        if (!guildId)
+            continue;
+
+        // Only a guild's FOUNDER/leader recruits for it.
+        Guild* guild = sGuildMgr->GetGuildById(guildId);
+        if (!guild || guild->GetLeaderGUID() != leader->GetGUID())
+            continue;
+
+        if (guild->GetMemberCount() >= cap)
+            continue;  // guild full - do not recruit
+
+        std::string const leaderPersonality = GetPersonality(leader->GetGUID());
+        float const baseChance = OngoingRecruitChanceFor(leaderPersonality);
+        if (baseChance <= 0.0f)
+            continue;  // not a leader/interactive personality
+
+        PersonalitySet const* fitSet = FitSetForLeader(leaderPersonality);
+        std::string const guildName = guild->GetName();
+        TeamId const team = leader->GetTeamId();
+        int32 slots = int32(cap) - int32(guild->GetMemberCount());
+
+        std::list<Player*> nearby;
+        Acore::AnyPlayerInObjectRangeCheck check(leader, range);
+        Acore::PlayerListSearcher<Acore::AnyPlayerInObjectRangeCheck> searcher(leader, nearby, check);
+        Cell::VisitObjects(leader, searcher, range);
+
+        for (Player* target : nearby)
+        {
+            if (slots <= 0)
+                break;  // guild is (about to be) full
+            if (!target || target == leader)
+                continue;
+            if (target->GetTeamId() != team)
+                continue;  // guild invites are same-faction
+            if (target->GetGuildId() || target->GetGuildIdInvited())
+                continue;  // already guilded, or already has a pending invite
+
+            // Per-(recruiter, target) cooldown so nobody is re-spammed.
+            auto const cdKey = std::make_pair(leader->GetGUID(), target->GetGUID());
+            {
+                std::lock_guard<std::mutex> lock(_mutex);
+                auto it = _lastInvite.find(cdKey);
+                if (it != _lastInvite.end() && now - it->second < cooldownMs)
+                    continue;
+            }
+
+            bool const targetIsBot = sPlayerbotsMgr.GetPlayerbotAI(target) != nullptr;
+
+            if (targetIsBot)
+            {
+                // Preserve elite purity: an elite guild only admits bots whose
+                // personality is in its fit-set. Casual guilds admit anyone.
+                if (fitSet && !fitSet->count(GetPersonality(target->GetGUID())))
+                    continue;
+
+                if (!roll_chance_f(baseChance))
+                    continue;
+
+                SendGuildInvite(leader, target);  // bot auto-accepts via its own packet handler
+                {
+                    std::lock_guard<std::mutex> lock(_mutex);
+                    _lastInvite[cdKey] = now;
+                }
+                --slots;
+                LOG_INFO("playerbots", "[OngoingRecruit] {} ({}) invited bot {} to '{}'", leader->GetName(),
+                         leaderPersonality, target->GetName(), guildName);
+
+                // Occasional in-voice say - audible recruitment without spam.
+                if (roll_chance_i(25))
+                    SpeakSituation(leader, nullptr,
+                                   Acore::StringFormat("you just recruited a fellow adventurer into your {} guild "
+                                                       "'{}' - say something short and welcoming",
+                                                       ArchetypeName(leaderPersonality), guildName),
+                                   false);
+            }
+            else
+            {
+                // The real player: always archetype-eligible, gated on sentiment;
+                // higher standing raises the chance. No auto-accept - they choose.
+                // Global cooldown first: never popup-spam the player across leaders.
+                if (playerCdMs && g_lastPlayerInviteMs && now - g_lastPlayerInviteMs < playerCdMs)
+                    continue;
+
+                float chance = baseChance;
+                if (GetSentiment(leader, target) >= 0.6f)
+                    chance += sentimentBonus;
+
+                if (!roll_chance_f(chance))
+                    continue;
+
+                SendGuildInvite(leader, target);
+                g_lastPlayerInviteMs = now;
+                {
+                    std::lock_guard<std::mutex> lock(_mutex);
+                    _lastInvite[cdKey] = now;
+                }
+                LOG_INFO("playerbots", "[OngoingRecruit] {} ({}) invited player {} to '{}'", leader->GetName(),
+                         leaderPersonality, target->GetName(), guildName);
+                SpeakSituation(leader, target,
+                               Acore::StringFormat("you're inviting {} to join your guild '{}' - give a short "
+                                                   "in-character pitch for why they should",
+                                                   target->GetName(), guildName),
+                               true);
+            }
+        }
+    }
+}
diff --git a/src/Bot/Factory/OngoingGuildRecruit.h b/src/Bot/Factory/OngoingGuildRecruit.h
new file mode 100644
index 00000000..3f0d19ac
--- /dev/null
+++ b/src/Bot/Factory/OngoingGuildRecruit.h
@@ -0,0 +1,54 @@
+/*
+ * Copyright (C) 2016+ AzerothCore <www.azerothcore.org>, released under GNU AGPL v3 license, you may redistribute it
+ * and/or modify it under version 3 of the License, or (at your option), any later version.
+ */
+
+#ifndef PLAYERBOTS_ONGOINGGUILDRECRUIT_H
+#define PLAYERBOTS_ONGOINGGUILDRECRUIT_H
+
+#include "ObjectGuid.h"
+
+#include <map>
+#include <mutex>
+#include <utility>
+
+// Ongoing, opportunistic guild recruitment. Where GuildRecruitmentEvent is a
+// one-shot realm-start push (leaders teleport to the newbie areas for ~15 min),
+// this runs for the whole life of the server as normal bot life happens.
+//
+// Every ~AiPlayerbot.OngoingRecruitIntervalSec, each online random bot that
+// FOUNDED a guild (it is the guild's leader) and has a leader / interactive
+// personality opportunistically sends guild invites to nearby unguilded BOTS
+// and to the nearby unguilded real PLAYER, in its own voice. Elite (raid / pvp)
+// guilds only admit bots whose personality is in the guild's fit-set, preserving
+// exactly the purity invariant PersonalityGuildFactory establishes; casual
+// guilds take anyone. The real player is always archetype-eligible and is gated
+// on the recruiter's sentiment toward them.
+//
+// Driven from the same world-thread tick as GuildRecruitmentEvent; cheap when
+// idle (a single map walk, heavy work throttled to the configured interval).
+class OngoingGuildRecruit
+{
+public:
+    static OngoingGuildRecruit& instance();
+
+    // World-thread tick driver; call every world update. Accumulates diff and
+    // does the bot scan + grid searches only once per configured interval.
+    void Update(uint32 diff);
+
+private:
+    OngoingGuildRecruit() = default;
+
+    void Tick();
+
+    uint32 _accumMs = 0;
+
+    // Per-(recruiter, target) last-invite time in ms (getMSTime) so a target is
+    // not re-spammed within AiPlayerbot.OngoingRecruitCooldownMin. Guarded even
+    // though the driver is world-thread only, mirroring the sibling sentiment /
+    // personality caches this collaborates with.
+    std::mutex _mutex;
+    std::map<std::pair<ObjectGuid, ObjectGuid>, uint32> _lastInvite;
+};
+
+#endif
diff --git a/src/Bot/Factory/PartyGuildFormation.cpp b/src/Bot/Factory/PartyGuildFormation.cpp
new file mode 100644
index 00000000..5ccce293
--- /dev/null
+++ b/src/Bot/Factory/PartyGuildFormation.cpp
@@ -0,0 +1,551 @@
+/*
+ * Copyright (C) 2016+ AzerothCore <www.azerothcore.org>, released under GNU AGPL v3 license, you may redistribute it
+ * and/or modify it under version 3 of the License, or (at your option), any later version.
+ */
+
+#include "PartyGuildFormation.h"
+
+#include "Common.h"
+#include "Config.h"
+#include "DatabaseEnv.h"
+#include "Group.h"
+#include "GroupMgr.h"
+#include "GroupScript.h"
+#include "Guild.h"
+#include "GuildMgr.h"
+#include "Log.h"
+#include "ObjectAccessor.h"
+#include "Opcodes.h"
+#include "Player.h"
+#include "PlayerScript.h"
+#include "PlayerbotGuildMgr.h"
+#include "PlayerbotMgr.h"
+#include "Random.h"
+#include "RandomPlayerbotFactory.h"
+#include "RandomPlayerbotMgr.h"
+#include "ScriptMgr.h"
+#include "StringFormat.h"
+#include "Timer.h"
+#include "WorldPacket.h"
+#include "WorldSession.h"
+
+#include <algorithm>
+#include <string>
+#include <unordered_map>
+#include <unordered_set>
+#include <vector>
+
+// Provided by mod-ollama-chat (same binary), weakly optional (null and the call
+// a no-op when the chat module is disabled). Same hooks PersonalityGuildFactory /
+// OngoingGuildRecruit use: a bot SAYS a short in-character line about a concrete
+// situation, and the founder's personality invents the applied guild name.
+[[gnu::weak]] void OllamaChat_SpeakSituation(Player* bot, Player* target, std::string const& situation, bool whisper);
+[[gnu::weak]] void OllamaChat_RenameGuildInVoice(Player* leader, uint32 guildId, std::string const& archetype);
+
+namespace
+{
+    void SpeakSituation(Player* bot, Player* target, std::string const& situation, bool whisper)
+    {
+        if (OllamaChat_SpeakSituation)
+            OllamaChat_SpeakSituation(bot, target, situation, whisper);
+    }
+
+    void RenameGuildInVoice(Player* leader, uint32 guildId, std::string const& archetype)
+    {
+        if (OllamaChat_RenameGuildInVoice)
+            OllamaChat_RenameGuildInVoice(leader, guildId, archetype);
+    }
+
+    // ---- Config (read live, same idiom as the sibling guild factories) -----
+    bool PgEnabled() { return sConfigMgr->GetOption<bool>("AiPlayerbot.PartyGuild.Enabled", true); }
+    uint32 PgMinMinutes() { return sConfigMgr->GetOption<uint32>("AiPlayerbot.PartyGuild.MinMinutes", 5); }
+    float PgBaseChance() { return sConfigMgr->GetOption<float>("AiPlayerbot.PartyGuild.BaseChance", 8.0f); }
+    float PgDeathPenalty() { return sConfigMgr->GetOption<float>("AiPlayerbot.PartyGuild.DeathPenalty", 6.0f); }
+    uint32 PgCheckIntervalSec() { return sConfigMgr->GetOption<uint32>("AiPlayerbot.PartyGuild.CheckIntervalSec", 120); }
+
+    // Outcome-adjusted formation chance: starts at BaseChance, reduced by
+    // DeathPenalty per death, floored at 0. A wipe drives it to 0.
+    float OutcomeChance(uint32 deaths)
+    {
+        float chance = PgBaseChance() - PgDeathPenalty() * float(deaths);
+        return chance < 0.0f ? 0.0f : chance;
+    }
+
+    using PersonalitySet = std::unordered_set<std::string>;
+
+    // Archetype leader sets - a verbatim mirror of PersonalityGuildFactory's
+    // (they live in that file's anonymous namespace), used only to derive the
+    // founder's archetype label for the in-voice guild rename.
+    PersonalitySet const RAID_LEADERS = {"HARDCORE_RAIDLEAD", "THEORYCRAFTER", "MIN_MAXER", "LORE_NERD"};
+    PersonalitySet const PVP_LEADERS  = {"ELITE_ARENA_PVPER", "PVP_TRASHTALKER", "DUELIST", "RAGER"};
+
+    char const* ArchetypeForPersonality(std::string const& personality)
+    {
+        if (RAID_LEADERS.count(personality))
+            return "raid";
+        if (PVP_LEADERS.count(personality))
+            return "pvp";
+        return "casual";
+    }
+
+    // How well a personality suits founding/leading a guild. The six named tiers
+    // come straight from the design; a spread of other recognised leader-ish
+    // personalities score lower; everything else scores 0 (no leader fit).
+    uint32 LeaderScore(std::string const& personality)
+    {
+        static std::unordered_map<std::string, uint32> const kScores = {
+            {"GUILD_RECRUITER", 100}, {"HEROIC_LEADER", 90}, {"HARDCORE_RAIDLEAD", 85},
+            {"MENTOR", 80},           {"WOW_MOM", 75},       {"ELITE_ARENA_PVPER", 70},
+            {"LFG_SPAMMER", 65},      {"CHILL_DAD", 60},     {"THEORYCRAFTER", 60},
+            {"JOLLY_BEER_LOVER", 55}, {"MIN_MAXER", 50},     {"LORE_NERD", 45},
+            {"DUELIST", 40},          {"PVP_TRASHTALKER", 40}, {"RAGER", 30},
+        };
+        auto it = kScores.find(personality);
+        return it != kScores.end() ? it->second : 0;
+    }
+
+    // guid -> mod-ollama-chat personality from the shared characters DB, guid-
+    // cached; empty results retried after 5 min. Mirrors OngoingGuildRecruit's
+    // GetPersonality exactly.
+    std::string GetPersonality(ObjectGuid guid)
+    {
+        static int hasTable = -1;
+        if (hasTable == -1)
+            hasTable = CharacterDatabase.Query(
+                          "SELECT 1 FROM information_schema.TABLES WHERE TABLE_SCHEMA = DATABASE() "
+                          "AND TABLE_NAME = 'mod_ollama_chat_personality'")
+                          ? 1
+                          : 0;
+        if (!hasTable)
+            return "";
+
+        ObjectGuid::LowType const low = guid.GetCounter();
+        uint32 const now = getMSTime();
+        static std::mutex cacheMutex;
+        static std::map<ObjectGuid::LowType, std::pair<std::string, uint32>> cache;
+        {
+            std::lock_guard<std::mutex> lock(cacheMutex);
+            auto it = cache.find(low);
+            if (it != cache.end() &&
+                (!it->second.first.empty() || now - it->second.second < 5 * MINUTE * IN_MILLISECONDS))
+                return it->second.first;
+        }
+
+        std::string personality;
+        if (QueryResult result = CharacterDatabase.Query(
+                "SELECT personality FROM mod_ollama_chat_personality WHERE guid = {}", low))
+            personality = (*result)[0].Get<std::string>();
+
+        std::lock_guard<std::mutex> lock(cacheMutex);
+        cache[low] = {personality, now};
+        return personality;
+    }
+
+    bool IsBotMember(Player* member) { return sPlayerbotsMgr.GetPlayerbotAI(member) != nullptr; }
+
+    // The online members of a group, resolved to live Player pointers (offline
+    // slots skipped). Used for qualification, founder pick, death routing.
+    std::vector<Player*> OnlineMembers(Group* group)
+    {
+        std::vector<Player*> members;
+        if (!group)
+            return members;
+        for (auto const& slot : group->GetMemberSlots())
+            if (Player* p = ObjectAccessor::FindPlayer(slot.guid))
+                members.push_back(p);
+        return members;
+    }
+
+    // A "party" = a group with a non-bot player member AND at least one random
+    // bot member. (Both evaluated over the currently-online members.)
+    bool PartyQualifies(Group* group)
+    {
+        bool hasRealPlayer = false;
+        bool hasRandomBot = false;
+        for (Player* member : OnlineMembers(group))
+        {
+            if (!IsBotMember(member))
+                hasRealPlayer = true;
+            else if (sRandomPlayerbotMgr.IsRandomBot(member))
+                hasRandomBot = true;
+        }
+        return hasRealPlayer && hasRandomBot;
+    }
+
+    // Queue a CMSG_GUILD_INVITE on the founder's session for `target` - exactly
+    // OngoingGuildRecruit::SendGuildInvite / GuildRecruitmentEvent::SendGuildInvite.
+    // A BOT target's own "guild invite" handler auto-accepts; a real player gets
+    // the dialog and chooses.
+    void SendGuildInvite(Player* founder, Player* target)
+    {
+        WorldSession* session = founder->GetSession();
+        if (!session)
+            return;
+
+        WorldPacket* data = new WorldPacket(CMSG_GUILD_INVITE);
+        *data << target->GetName();
+        session->QueuePacket(data);
+    }
+}  // namespace
+
+PartyGuildFormation& PartyGuildFormation::instance()
+{
+    static PartyGuildFormation instance;
+    return instance;
+}
+
+void PartyGuildFormation::OnPartyChanged(Group* group)
+{
+    if (!PgEnabled() || !group)
+        return;
+
+    ObjectGuid const key = group->GetGUID();
+    bool const qualifies = PartyQualifies(group);
+
+    std::lock_guard<std::mutex> lock(_mutex);
+    auto it = _parties.find(key);
+    if (qualifies)
+    {
+        if (it == _parties.end())
+        {
+            PartyState state;
+            state.startMs = getMSTime();
+            _parties.emplace(key, state);  // fresh party - start its clock
+        }
+        // Already tracked: keep the original start time / counters.
+    }
+    else if (it != _parties.end() && !it->second.formed)
+    {
+        _parties.erase(it);  // stopped qualifying before it formed - forget it
+    }
+}
+
+void PartyGuildFormation::OnPartyDisband(ObjectGuid groupGuid)
+{
+    std::lock_guard<std::mutex> lock(_mutex);
+    _parties.erase(groupGuid);
+}
+
+void PartyGuildFormation::OnMemberDeath(Player* member)
+{
+    if (!PgEnabled() || !member)
+        return;
+
+    Group* group = member->GetGroup();
+    if (!group)
+        return;
+
+    std::lock_guard<std::mutex> lock(_mutex);
+    auto it = _parties.find(group->GetGUID());
+    if (it != _parties.end() && !it->second.formed)
+        ++it->second.deaths;
+}
+
+void PartyGuildFormation::OnMemberQuestComplete(Player* member)
+{
+    if (!PgEnabled() || !member)
+        return;
+
+    Group* group = member->GetGroup();
+    if (!group)
+        return;
+
+    std::lock_guard<std::mutex> lock(_mutex);
+    auto it = _parties.find(group->GetGUID());
+    if (it != _parties.end() && !it->second.formed)
+        ++it->second.questCompletions;
+}
+
+void PartyGuildFormation::Update(uint32 diff)
+{
+    if (!PgEnabled())
+        return;
+
+    uint32 const intervalMs = PgCheckIntervalSec() * IN_MILLISECONDS;
+    if (intervalMs == 0)
+        return;
+
+    _accumMs += diff;
+    if (_accumMs < intervalMs)
+        return;
+    _accumMs = 0;
+
+    uint32 const minMs = PgMinMinutes() * MINUTE * IN_MILLISECONDS;
+    uint32 const now = getMSTime();
+
+    // Snapshot the groups that pass the gate + random roll under the lock, then
+    // do the (heavier) forming outside it. Also reap entries whose group is gone.
+    std::vector<ObjectGuid> toForm;
+    std::vector<ObjectGuid> toErase;
+    {
+        std::lock_guard<std::mutex> lock(_mutex);
+        for (auto const& kv : _parties)
+        {
+            if (kv.second.formed)
+                continue;
+
+            Group* group = sGroupMgr->GetGroupByGUID(kv.first.GetCounter());
+            if (!group)
+            {
+                toErase.push_back(kv.first);
+                continue;
+            }
+            if (now - kv.second.startMs < minMs)
+                continue;
+
+            if (roll_chance_f(OutcomeChance(kv.second.deaths)))
+                toForm.push_back(kv.first);
+        }
+        for (ObjectGuid const& guid : toErase)
+            _parties.erase(guid);
+    }
+
+    for (ObjectGuid const& guid : toForm)
+        if (Group* group = sGroupMgr->GetGroupByGUID(guid.GetCounter()))
+            TryForm(group, /*playerInitiated=*/false);
+}
+
+void PartyGuildFormation::OnPlayerRequest(Player* player)
+{
+    if (!PgEnabled() || !player)
+        return;
+
+    Group* group = player->GetGroup();
+    if (!group)
+        return;
+
+    // Only act if this group is a tracked qualifying party (has bots to answer).
+    uint32 deaths = 0;
+    uint32 startMs = 0;
+    {
+        std::lock_guard<std::mutex> lock(_mutex);
+        auto it = _parties.find(group->GetGUID());
+        if (it == _parties.end() || it->second.formed)
+            return;
+        deaths = it->second.deaths;
+        startMs = it->second.startMs;
+    }
+
+    uint32 const minMs = PgMinMinutes() * MINUTE * IN_MILLISECONDS;
+    bool const oldEnough = (getMSTime() - startMs) >= minMs;
+    bool const notWiped = OutcomeChance(deaths) > 0.0f;
+
+    // Initiation bypasses the random roll but keeps the >= MinMinutes + not-wiped
+    // gate. If it passes, form now; otherwise a party bot declines in-voice.
+    if (oldEnough && notWiped)
+    {
+        TryForm(group, /*playerInitiated=*/true);
+        return;
+    }
+
+    // Pick a random bot member to voice the decline.
+    Player* speaker = nullptr;
+    for (Player* member : OnlineMembers(group))
+    {
+        if (IsBotMember(member))
+        {
+            speaker = member;
+            break;
+        }
+    }
+    if (!speaker)
+        return;
+
+    if (!oldEnough)
+        SpeakSituation(speaker, nullptr,
+                       "a groupmate suggested your party form a guild, but you've only just met - gently say give it "
+                       "time",
+                       false);
+    else  // wiped
+        SpeakSituation(speaker, nullptr,
+                       "a groupmate suggested your party form a guild right after a rough run full of deaths - say "
+                       "maybe later",
+                       false);
+}
+
+bool PartyGuildFormation::TryForm(Group* group, bool playerInitiated)
+{
+    if (!group)
+        return false;
+
+    ObjectGuid const key = group->GetGUID();
+
+    // Claim the party (guard against double-create) and read its death count.
+    uint32 deaths = 0;
+    {
+        std::lock_guard<std::mutex> lock(_mutex);
+        auto it = _parties.find(key);
+        if (it == _parties.end() || it->second.formed)
+            return false;
+        deaths = it->second.deaths;
+        it->second.formed = true;  // world-thread only, but claim before we build
+    }
+
+    auto unclaim = [&]()
+    {
+        std::lock_guard<std::mutex> lock(_mutex);
+        auto it = _parties.find(key);
+        if (it != _parties.end())
+            it->second.formed = false;
+    };
+
+    std::vector<Player*> const members = OnlineMembers(group);
+
+    // INVARIANT: don't form if the crew is already guilded together (the real
+    // player and any bot already share a guild).
+    Player* realPlayer = nullptr;
+    for (Player* member : members)
+        if (!IsBotMember(member))
+        {
+            realPlayer = member;
+            break;
+        }
+    if (realPlayer && realPlayer->GetGuildId())
+    {
+        for (Player* member : members)
+            if (member != realPlayer && member->GetGuildId() == realPlayer->GetGuildId())
+            {
+                unclaim();
+                return false;  // already in one guild together
+            }
+    }
+
+    // FOUNDER = the best guild-leader personality among the GUILDLESS bot members
+    // (a founder must be guildless or CreateGuild fails). Best score wins; if none
+    // scores, the top guildless bot still founds - it's the player's chosen crew.
+    Player* founder = nullptr;
+    uint32 bestScore = 0;
+    std::string founderPersonality;
+    for (Player* member : members)
+    {
+        if (!IsBotMember(member) || member->GetGuildId())
+            continue;
+
+        std::string const personality = GetPersonality(member->GetGUID());
+        uint32 const score = LeaderScore(personality);
+        if (!founder || score > bestScore)
+        {
+            founder = member;
+            bestScore = score;
+            founderPersonality = personality;
+        }
+    }
+    if (!founder)
+    {
+        unclaim();
+        return false;  // no guildless bot to found the guild
+    }
+
+    // Start with a random unused pool name; the founder's LLM voice renames it.
+    std::string const initialName = RandomPlayerbotFactory::CreateRandomGuildName();
+    if (initialName.empty() || !PlayerbotGuildMgr::instance().CreateGuild(founder, initialName))
+    {
+        LOG_ERROR("playerbots", "[PartyGuild] failed to create guild (founder {})", founder->GetName());
+        unclaim();
+        return false;
+    }
+
+    Guild* guild = sGuildMgr->GetGuildByName(initialName);
+    if (!guild)
+    {
+        unclaim();
+        return false;
+    }
+
+    // Suggest names in party chat first (1-2 bots) so the player sees ideas; the
+    // founder's async LLM rename is the one actually applied.
+    uint32 suggested = 0;
+    for (Player* member : members)
+    {
+        if (suggested >= 2)
+            break;
+        if (member == founder || !IsBotMember(member))
+            continue;
+        SpeakSituation(member, nullptr,
+                       "your party had a great run together and is forming a guild - suggest a cool guild name", false);
+        ++suggested;
+    }
+
+    // Invite EVERY other party member: bots (auto-accept) and the real player
+    // (their choice). Intentional elite-purity exception - this is the player's
+    // chosen crew, so invite regardless of personality fit.
+    for (Player* member : members)
+    {
+        if (member == founder)
+            continue;
+        if (member->GetGuildId())
+            continue;  // already guilded elsewhere - can't join a second
+        SendGuildInvite(founder, member);
+    }
+
+    PlayerbotGuildMgr::instance().OnGuildUpdate(guild);
+
+    // Founder's personality invents + applies the real guild name (async).
+    RenameGuildInVoice(founder, guild->GetId(), ArchetypeForPersonality(founderPersonality));
+
+    LOG_INFO("playerbots", "[PartyGuild] party of {} formed guild (founder {} {}, {} deaths, {})", members.size(),
+             founder->GetName(), founderPersonality.empty() ? "no-personality" : founderPersonality, deaths,
+             playerInitiated ? "player-initiated" : "spontaneous");
+    return true;
+}
+
+// ---------------------------------------------------------------------------
+// Weak-symbol entry point for mod-ollama-chat's party-chat "let's guild up"
+// intent. Plain global (external linkage) so the module's `[[gnu::weak]]` extern
+// binds to it. World thread (called synchronously from the chat handler).
+// ---------------------------------------------------------------------------
+void PartyGuild_OnPlayerRequest(Player* player)
+{
+    PartyGuildFormation::instance().OnPlayerRequest(player);
+}
+
+// ---------------------------------------------------------------------------
+// Scripts that feed the singleton. GroupScript membership changes maintain the
+// party registry; the PlayerScript counts member deaths / quest completions.
+// Empty enabled-hook lists (all hooks on) mirror mod-ollama-chat's SentimentOnGroup
+// GroupScript and ChatOnDeath PlayerScript, which use the same OnAddMember /
+// OnPlayerJustDied hooks in this fork.
+// ---------------------------------------------------------------------------
+class PartyGuildGroupScript : public GroupScript
+{
+public:
+    PartyGuildGroupScript() : GroupScript("PartyGuildGroupScript") {}
+
+    void OnCreate(Group* group, Player* /*leader*/) override { PartyGuildFormation::instance().OnPartyChanged(group); }
+
+    void OnAddMember(Group* group, ObjectGuid /*guid*/) override
+    {
+        PartyGuildFormation::instance().OnPartyChanged(group);
+    }
+
+    void OnRemoveMember(Group* group, ObjectGuid /*guid*/, RemoveMethod /*method*/, ObjectGuid /*kicker*/,
+                        char const* /*reason*/) override
+    {
+        PartyGuildFormation::instance().OnPartyChanged(group);
+    }
+
+    void OnDisband(Group* group) override
+    {
+        if (group)
+            PartyGuildFormation::instance().OnPartyDisband(group->GetGUID());
+    }
+};
+
+class PartyGuildPlayerScript : public PlayerScript
+{
+public:
+    PartyGuildPlayerScript() : PlayerScript("PartyGuildPlayerScript") {}
+
+    void OnPlayerJustDied(Player* player) override { PartyGuildFormation::instance().OnMemberDeath(player); }
+
+    void OnPlayerCompleteQuest(Player* player, Quest const* /*quest*/) override
+    {
+        PartyGuildFormation::instance().OnMemberQuestComplete(player);
+    }
+};
+
+void AddSC_PartyGuildFormation()
+{
+    new PartyGuildGroupScript();
+    new PartyGuildPlayerScript();
+}
diff --git a/src/Bot/Factory/PartyGuildFormation.h b/src/Bot/Factory/PartyGuildFormation.h
new file mode 100644
index 00000000..1069007f
--- /dev/null
+++ b/src/Bot/Factory/PartyGuildFormation.h
@@ -0,0 +1,90 @@
+/*
+ * Copyright (C) 2016+ AzerothCore <www.azerothcore.org>, released under GNU AGPL v3 license, you may redistribute it
+ * and/or modify it under version 3 of the License, or (at your option), any later version.
+ */
+
+#ifndef PLAYERBOTS_PARTYGUILDFORMATION_H
+#define PLAYERBOTS_PARTYGUILDFORMATION_H
+
+#include "ObjectGuid.h"
+
+#include <map>
+#include <mutex>
+
+class Group;
+class Player;
+
+// Emergent PARTY -> GUILD formation.
+//
+// Every GROUP that contains the real player and at least one random bot (a
+// "party") is tracked from the moment it qualifies: its start time and the
+// number of member deaths during its life (quest completions too, for flavor).
+// When such a party has been alive long enough and the run went WELL, it can
+// spontaneously become a guild founded by its most leader-suited bot, which
+// then invites the whole crew (bots auto-accept; the real player chooses).
+//
+// Two trigger paths:
+//   (1) SPONTANEOUS - a periodic world-thread tick rolls an outcome-adjusted
+//       chance (starts at PartyGuild.BaseChance, minus PartyGuild.DeathPenalty
+//       per death); a clean, long-lived party is far likelier than a wiped one.
+//   (2) PLAYER-INITIATED - the real player says a "let's guild up" line in party
+//       chat; mod-ollama-chat routes it here via the weak PartyGuild_OnPlayerRequest
+//       symbol. Initiation bypasses the random roll but still requires the
+//       >= MinMinutes + not-wiped gate; otherwise a party bot answers in-voice.
+//
+// All state lives in a single world-thread-driven singleton. The GroupScript
+// and PlayerScript that feed it are registered from AddPlayerbotsScripts().
+class PartyGuildFormation
+{
+public:
+    static PartyGuildFormation& instance();
+
+    // --- GroupScript feed (world thread) ---------------------------------
+    // Re-evaluate a group's membership: register it the moment it becomes a
+    // qualifying party, or drop an un-formed entry that stopped qualifying.
+    void OnPartyChanged(Group* group);
+    // Forget a disbanded group entirely (clears any registry state).
+    void OnPartyDisband(ObjectGuid groupGuid);
+
+    // --- PlayerScript feed (world thread) --------------------------------
+    // Count a member death against the party the dead member belongs to.
+    void OnMemberDeath(Player* member);
+    // Count a member quest completion (flavor only; not used in the odds).
+    void OnMemberQuestComplete(Player* member);
+
+    // --- Drivers ----------------------------------------------------------
+    // World-thread spontaneous tick; call every world update.
+    void Update(uint32 diff);
+    // Player-initiated path (invoked from mod-ollama-chat's party-chat intent).
+    void OnPlayerRequest(Player* player);
+
+private:
+    PartyGuildFormation() = default;
+
+    struct PartyState
+    {
+        uint32 startMs = 0;            // getMSTime() when the party first qualified
+        uint32 deaths = 0;             // member deaths during the party's life
+        uint32 questCompletions = 0;   // member quest completions (flavor)
+        bool formed = false;           // guard against double-forming one group
+    };
+
+    // Attempt to actually found the guild for `group` (founder pick + create +
+    // invites + async name). Returns true on success; sets the party formed.
+    bool TryForm(Group* group, bool playerInitiated);
+
+    std::mutex _mutex;                            // sibling-consistent guard
+    std::map<ObjectGuid, PartyState> _parties;    // group GUID -> tracked state
+    uint32 _accumMs = 0;                          // spontaneous-tick accumulator
+};
+
+// Plain global (external linkage) so mod-ollama-chat's `[[gnu::weak]]` extern
+// binds to it in the same binary. Routes a real player's party-chat "let's
+// guild up" line to the player-initiated formation path.
+void PartyGuild_OnPlayerRequest(Player* player);
+
+// Registration entry (AddSC-style). Registers the GroupScript + PlayerScript
+// that feed the singleton; call from AddPlayerbotsScripts().
+void AddSC_PartyGuildFormation();
+
+#endif
diff --git a/src/Bot/Factory/PersonalityGuildFactory.cpp b/src/Bot/Factory/PersonalityGuildFactory.cpp
new file mode 100644
index 00000000..366bd357
--- /dev/null
+++ b/src/Bot/Factory/PersonalityGuildFactory.cpp
@@ -0,0 +1,380 @@
+/*
+ * Copyright (C) 2016+ AzerothCore <www.azerothcore.org>, released under GNU AGPL v3 license, you may redistribute it
+ * and/or modify it under version 3 of the License, or (at your option), any later version.
+ */
+
+#include "PersonalityGuildFactory.h"
+
+#include "Common.h"
+#include "Config.h"
+#include "DatabaseEnv.h"
+#include "Guild.h"
+#include "GuildMgr.h"
+#include "Log.h"
+#include "ObjectAccessor.h"
+#include "Player.h"
+#include "PlayerbotAIConfig.h"
+#include "PlayerbotGuildMgr.h"
+#include "Random.h"
+#include "RandomPlayerbotFactory.h"
+#include "RandomPlayerbotMgr.h"
+#include "StringFormat.h"
+
+#include <algorithm>
+#include <string>
+#include <unordered_map>
+#include <unordered_set>
+#include <vector>
+
+// Provided by mod-ollama-chat (same binary): right after a personality guild is
+// formed, have the bot LEADER's personality invent a better guild name via the
+// local LLM and rename the guild asynchronously. Weakly optional - if the chat
+// module is disabled the call is a no-op stub below.
+[[gnu::weak]] void OllamaChat_RenameGuildInVoice(Player* leader, uint32 guildId, std::string const& archetype);
+static void SpeakGuildName(Player* leader, uint32 guildId, std::string const& archetype)
+{
+    if (OllamaChat_RenameGuildInVoice)
+        OllamaChat_RenameGuildInVoice(leader, guildId, archetype);
+}
+
+namespace
+{
+    enum class GuildArchetype : uint8
+    {
+        Raid = 0,
+        Pvp,
+        Casual
+    };
+
+    char const* ArchetypeName(GuildArchetype archetype)
+    {
+        switch (archetype)
+        {
+            case GuildArchetype::Raid:
+                return "raid";
+            case GuildArchetype::Pvp:
+                return "pvp";
+            default:
+                return "casual";
+        }
+    }
+
+    using PersonalitySet = std::unordered_set<std::string>;
+
+    // --- Leader personalities: a guild's GM must be one of these ------------
+    PersonalitySet const RAID_LEADERS   = {"HARDCORE_RAIDLEAD", "THEORYCRAFTER", "MIN_MAXER", "LORE_NERD"};
+    PersonalitySet const PVP_LEADERS    = {"ELITE_ARENA_PVPER", "PVP_TRASHTALKER", "DUELIST", "RAGER"};
+    PersonalitySet const CASUAL_LEADERS = {"WOW_MOM",     "MENTOR",           "GUILD_RECRUITER",
+                                           "CHILL_DAD",   "JOLLY_BEER_LOVER", "HEROIC_LEADER"};
+
+    // --- Elite membership fit-sets: fit is REQUIRED, no randomness ----------
+    // Leader personalities are unioned in so a leader always fits its own guild.
+    PersonalitySet const RAID_FIT = {"RAIDER",     "THEORYCRAFTER",      "MIN_MAXER",   "HARDCORE_RAIDLEAD",
+                                     "HEALER_MAIN", "SPEEDRUNNER",       "ACHIEVEMENT_HUNTER", "LORE_NERD"};
+    PersonalitySet const PVP_FIT  = {"PVP_HARDCORE", "PVP_TRASHTALKER", "DUELIST",
+                                     "ELITE_ARENA_PVPER", "RAGER", "EDGE_LORD"};
+
+    struct Candidate
+    {
+        ObjectGuid guid;
+        uint32 lowGuid = 0;
+        uint32 level = 0;
+        uint8 team = 0;  // TeamId
+        std::string personality;
+    };
+
+    // Bulk-load guid -> personality from the shared characters DB (one query).
+    void LoadPersonalities(std::unordered_map<uint32, std::string>& out)
+    {
+        QueryResult result = CharacterDatabase.Query("SELECT guid, personality FROM mod_ollama_chat_personality");
+        if (!result)
+            return;
+
+        do
+        {
+            Field* fields = result->Fetch();
+            out[fields[0].Get<uint32>()] = fields[1].Get<std::string>();
+        } while (result->NextRow());
+    }
+
+    // Build the working set: currently-online, guildless random bots, tagged
+    // with level / faction / personality. Mirrors the random-bot pool query
+    // used by RandomPlayerbotFactory::CreateRandomArenaTeams.
+    void LoadOnlineGuildlessBots(std::vector<Candidate>& out,
+                                 std::unordered_map<uint32, std::string> const& personalities)
+    {
+        PlayerbotsDatabasePreparedStatement* stmt =
+            PlayerbotsDatabase.GetPreparedStatement(PLAYERBOTS_SEL_RANDOM_BOTS_BOT);
+        stmt->SetData(0, "add");
+        PreparedQueryResult result = PlayerbotsDatabase.Query(stmt);
+        if (!result)
+            return;
+
+        do
+        {
+            Field* fields = result->Fetch();
+            uint32 lowGuid = fields[0].Get<uint32>();
+            ObjectGuid guid = ObjectGuid::Create<HighGuid::Player>(lowGuid);
+
+            Player* bot = ObjectAccessor::FindConnectedPlayer(guid);
+            if (!bot || bot->GetGuildId())
+                continue;
+
+            Candidate candidate;
+            candidate.guid = guid;
+            candidate.lowGuid = lowGuid;
+            candidate.level = bot->GetLevel();
+            candidate.team = bot->GetTeamId();
+            auto it = personalities.find(lowGuid);
+            candidate.personality = (it != personalities.end()) ? it->second : "";
+            out.push_back(std::move(candidate));
+        } while (result->NextRow());
+    }
+
+    // Small themed-name generator. Falls back to the module's unused-name pool
+    // (playerbots_guild_names) if it cannot mint a short, unique themed name.
+    std::string MakeThemedName(GuildArchetype archetype)
+    {
+        static std::vector<std::string> const raidPrefix   = {"Wrath",   "Nightfall", "Ascended", "Molten",
+                                                              "Ruin",    "Vengeance", "Eternal",  "Heroic"};
+        static std::vector<std::string> const raidSuffix   = {"Legion",     "Covenant", "Vanguard",
+                                                              "Dominion",   "Ascendancy", "Reckoning"};
+        static std::vector<std::string> const pvpPrefix    = {"Savage",  "Bloodthirst", "Rampage", "Cutthroat",
+                                                              "Feral",   "Merciless",   "Brutal"};
+        static std::vector<std::string> const pvpSuffix    = {"Warband",  "Marauders", "Onslaught",
+                                                              "Gladiators", "Reavers", "Bloodbath"};
+        static std::vector<std::string> const casualPrefix = {"Cozy",   "Chill",    "Sunday",   "Friendly",
+                                                              "Hearth", "Campfire", "Merry"};
+        static std::vector<std::string> const casualSuffix = {"Questers", "Wanderers", "Crew",
+                                                              "Company",  "Friends",   "Adventurers"};
+
+        std::vector<std::string> const* prefix = &casualPrefix;
+        std::vector<std::string> const* suffix = &casualSuffix;
+        switch (archetype)
+        {
+            case GuildArchetype::Raid:
+                prefix = &raidPrefix;
+                suffix = &raidSuffix;
+                break;
+            case GuildArchetype::Pvp:
+                prefix = &pvpPrefix;
+                suffix = &pvpSuffix;
+                break;
+            default:
+                break;
+        }
+
+        for (uint32 attempt = 0; attempt < 20; ++attempt)
+        {
+            std::string name = Acore::StringFormat("{} {}", (*prefix)[urand(0, prefix->size() - 1)],
+                                                   (*suffix)[urand(0, suffix->size() - 1)]);
+            if (name.size() > 24)  // WoW guild-name limit
+                continue;
+            if (!sGuildMgr->GetGuildByName(name))
+                return name;
+        }
+
+        return RandomPlayerbotFactory::CreateRandomGuildName();
+    }
+}  // namespace
+
+void PersonalityGuildFactory::FormPersonalityGuilds()
+{
+    // Seed the world with a few guilds at startup; the rest emerge gradually via
+    // UpdateEmergentFounding + the ongoing recruiter. Runs at most once per process.
+    static bool seeded = false;
+    if (seeded)
+        return;
+    seeded = true;
+
+    uint32 const seedCount = sConfigMgr->GetOption<uint32>("AiPlayerbot.PersonalityGuild.SeedCount", 2);
+
+    uint32 formed = 0;
+    for (uint32 i = 0; i < seedCount; ++i)
+        if (TryFoundOneGuild())
+            ++formed;
+
+    LOG_INFO("playerbots", "[PersonalityGuild] Seeded {} of {} initial guilds; the rest form over time", formed,
+             seedCount);
+}
+
+bool PersonalityGuildFactory::TryFoundOneGuild()
+{
+    uint32 const raidCount = sConfigMgr->GetOption<uint32>("AiPlayerbot.PersonalityGuild.RaidCount", 4);
+    uint32 const pvpCount = sConfigMgr->GetOption<uint32>("AiPlayerbot.PersonalityGuild.PvpCount", 3);
+    uint32 const casualCount = sConfigMgr->GetOption<uint32>("AiPlayerbot.PersonalityGuild.CasualCount", 8);
+    uint32 const leaderMinLevel = sConfigMgr->GetOption<uint32>("AiPlayerbot.PersonalityGuild.LeaderMinLevel", 10);
+    float const casualJoinChance = sConfigMgr->GetOption<float>("AiPlayerbot.PersonalityGuild.CasualJoinChance", 40.0f);
+    uint32 const maxMembers = std::max<uint32>(2, sPlayerbotAIConfig.randomBotGuildSizeMax);
+    uint32 const foundInitial = sConfigMgr->GetOption<uint32>("AiPlayerbot.PersonalityGuild.FoundInitialMembers", 4);
+    uint32 const initialCap = std::min(std::max<uint32>(1, foundInitial), maxMembers);
+
+    // Fresh snapshot of the CURRENT online, guildless random bots + personalities
+    // (self-contained, like OngoingGuildRecruit::Tick - no captured batch state).
+    std::unordered_map<uint32, std::string> personalities;
+    LoadPersonalities(personalities);
+
+    std::vector<Candidate> pool;
+    LoadOnlineGuildlessBots(pool, personalities);
+    if (pool.empty())
+        return false;
+
+    // Archetype preference weighted by the configured Raid:Pvp:Casual mix (defaults
+    // ~1:1:2). Build a fallback order so the tick still founds something when the
+    // primary archetype has no eligible leader online right now.
+    uint32 weights[3] = {raidCount, pvpCount, casualCount};
+    uint32 total = weights[0] + weights[1] + weights[2];
+    if (total == 0)
+    {
+        weights[0] = 1;
+        weights[1] = 1;
+        weights[2] = 2;
+        total = 4;
+    }
+
+    uint32 roll = urand(0, total - 1);
+    uint32 primary = 0;
+    for (; primary < 2; ++primary)  // last bucket (casual) is the default remainder
+    {
+        if (roll < weights[primary])
+            break;
+        roll -= weights[primary];
+    }
+
+    GuildArchetype const archetypes[3] = {GuildArchetype::Raid, GuildArchetype::Pvp, GuildArchetype::Casual};
+    std::vector<GuildArchetype> order;
+    order.push_back(archetypes[primary]);
+    for (uint32 i = 0; i < 3; ++i)
+        if (i != primary)
+            order.push_back(archetypes[i]);
+
+    for (GuildArchetype const archetype : order)
+    {
+        PersonalitySet const* leaderSet = &CASUAL_LEADERS;
+        PersonalitySet const* fitSet = nullptr;  // casual: any personality fits
+        switch (archetype)
+        {
+            case GuildArchetype::Raid:
+                leaderSet = &RAID_LEADERS;
+                fitSet = &RAID_FIT;
+                break;
+            case GuildArchetype::Pvp:
+                leaderSet = &PVP_LEADERS;
+                fitSet = &PVP_FIT;
+                break;
+            default:
+                break;
+        }
+        bool const casual = (fitSet == nullptr);
+
+        // Find an eligible ONLINE guildless leader whose personality fits this
+        // archetype's leader set and meets the minimum level.
+        Candidate leader;
+        Player* leaderPlayer = nullptr;
+        for (Candidate const& candidate : pool)
+        {
+            if (candidate.level < leaderMinLevel || !leaderSet->count(candidate.personality))
+                continue;
+
+            Player* player = ObjectAccessor::FindConnectedPlayer(candidate.guid);
+            if (!player || player->GetGuildId())
+                continue;
+
+            leader = candidate;
+            leaderPlayer = player;
+            break;
+        }
+        if (!leaderPlayer)
+            continue;  // no eligible leader for this archetype - try the next
+
+        std::string name = MakeThemedName(archetype);
+        if (name.empty())
+            continue;
+
+        if (!PlayerbotGuildMgr::instance().CreateGuild(leaderPlayer, name))
+        {
+            LOG_ERROR("playerbots", "[EmergentGuild] Failed to create {} guild '{}' led by {}",
+                      ArchetypeName(archetype), name, leaderPlayer->GetName());
+            continue;
+        }
+
+        // Seed a SMALL guild (headroom stays for the ongoing recruiter to grow it
+        // and for the real player to join). Never exceed maxMembers. Elite: strictly
+        // fit-set, same faction. Casual: any same-faction bot, admitted with chance.
+        uint32 memberCount = 1;  // leader
+        Guild* guild = sGuildMgr->GetGuildByName(name);
+        if (guild)
+        {
+            std::unordered_set<uint32> committed;
+            committed.insert(leader.lowGuid);
+            for (Candidate const& candidate : pool)
+            {
+                if (memberCount >= initialCap)
+                    break;
+                if (committed.count(candidate.lowGuid) || candidate.team != leader.team)
+                    continue;
+                if (casual)
+                {
+                    if (!roll_chance_f(casualJoinChance))  // randomness: anyone may roll in
+                        continue;
+                }
+                else if (!fitSet->count(candidate.personality))
+                {
+                    continue;  // elite purity: fit-set only
+                }
+
+                Player* member = ObjectAccessor::FindConnectedPlayer(candidate.guid);
+                if (!member || member->GetGuildId())
+                {
+                    committed.insert(candidate.lowGuid);
+                    continue;
+                }
+
+                if (guild->AddMember(candidate.guid, uint8(urand(GR_OFFICER, GR_INITIATE))))
+                {
+                    committed.insert(candidate.lowGuid);
+                    ++memberCount;
+                }
+            }
+            PlayerbotGuildMgr::instance().OnGuildUpdate(guild);
+            SpeakGuildName(leaderPlayer, guild->GetId(), ArchetypeName(archetype));
+        }
+
+        LOG_INFO("playerbots", "[EmergentGuild] founded {} guild '{}' led by {} ({})", ArchetypeName(archetype), name,
+                 leaderPlayer->GetName(), leader.personality);
+        return true;
+    }
+
+    return false;
+}
+
+void PersonalityGuildFactory::UpdateEmergentFounding(uint32 diff)
+{
+    uint32 const intervalMs =
+        sConfigMgr->GetOption<uint32>("AiPlayerbot.PersonalityGuild.FoundIntervalSec", 300) * IN_MILLISECONDS;
+    if (intervalMs == 0)
+        return;
+
+    // World-thread only (PlayerbotsWorldScript::OnUpdate) - a plain static suffices.
+    static uint32 accumMs = 0;
+    accumMs += diff;
+    if (accumMs < intervalMs)
+        return;
+    accumMs = 0;
+
+    uint32 const targetCount = sConfigMgr->GetOption<uint32>("AiPlayerbot.PersonalityGuild.TargetCount", 12);
+
+    // Count distinct random-bot guilds (every guild an online random bot belongs
+    // to). Cheap, and mirrors the ongoing recruiter's bot-pool walk.
+    std::unordered_set<uint32> guildIds;
+    for (auto const& kv : sRandomPlayerbotMgr.GetAllBots())
+    {
+        Player* bot = kv.second;
+        if (bot && bot->GetGuildId())
+            guildIds.insert(bot->GetGuildId());
+    }
+    if (guildIds.size() >= targetCount)
+        return;
+
+    TryFoundOneGuild();
+}
diff --git a/src/Bot/Factory/PersonalityGuildFactory.h b/src/Bot/Factory/PersonalityGuildFactory.h
new file mode 100644
index 00000000..7bf85780
--- /dev/null
+++ b/src/Bot/Factory/PersonalityGuildFactory.h
@@ -0,0 +1,39 @@
+/*
+ * Copyright (C) 2016+ AzerothCore <www.azerothcore.org>, released under GNU AGPL v3 license, you may redistribute it
+ * and/or modify it under version 3 of the License, or (at your option), any later version.
+ */
+
+#ifndef PLAYERBOTS_PERSONALITYGUILDFACTORY_H
+#define PLAYERBOTS_PERSONALITYGUILDFACTORY_H
+
+#include "Define.h"
+
+class PersonalityGuildFactory
+{
+public:
+    // Seeds the world with a few personality guilds at startup (AiPlayerbot.
+    // PersonalityGuild.SeedCount), then leaves the rest to emerge over time. Runs
+    // at most once per server process (guarded internally), so it is safe to call
+    // from a bot-login hook.
+    static void FormPersonalityGuilds();
+
+    // Founds ONE themed, personality-driven guild (raid / pvp / casual) on demand
+    // from a FRESH snapshot of the currently-online, guildless random bots. Picks
+    // an archetype weighted by the configured Raid:Pvp:Casual mix, finds an online
+    // guildless leader whose mod-ollama-chat personality is in that archetype's
+    // leader set (>= LeaderMinLevel), creates the guild, then seeds it SMALL (up to
+    // FoundInitialMembers members): elite guilds admit only fit-set same-faction
+    // bots, casual guilds admit any same-faction bot with a join chance. The
+    // ongoing recruiter grows it the rest of the way and lets the real player join.
+    // Returns false if no eligible leader is online. Self-contained; safe to call
+    // repeatedly from a world tick.
+    static bool TryFoundOneGuild();
+
+    // World-thread tick driver for emergent formation: every AiPlayerbot.
+    // PersonalityGuild.FoundIntervalSec, founds one more guild while the number of
+    // random-bot guilds is below AiPlayerbot.PersonalityGuild.TargetCount. Call
+    // every world update.
+    static void UpdateEmergentFounding(uint32 diff);
+};
+
+#endif
diff --git a/src/Bot/RandomPlayerbotMgr.cpp b/src/Bot/RandomPlayerbotMgr.cpp
index 5910c2f6..9e88e821 100644
--- a/src/Bot/RandomPlayerbotMgr.cpp
+++ b/src/Bot/RandomPlayerbotMgr.cpp
@@ -40,6 +40,7 @@
 #include "RaceMgr.h"
 #include "Random.h"
 #include "RandomPlayerbotFactory.h"
+#include "PersonalityGuildFactory.h"
 #include "ServerFacade.h"
 #include "SharedDefines.h"
 #include "TravelMgr.h"
@@ -2540,6 +2541,8 @@ void RandomPlayerbotMgr::OnBotLoginInternal(Player* const bot)
         if (playerBots.size() == sRandomPlayerbotMgr.GetMaxAllowedBotCount())
         {
             _isBotLogging = false;
+            // whole population is online now - form personality-themed guilds
+            PersonalityGuildFactory::FormPersonalityGuilds();
         }
     }
 
diff --git a/src/PlayerbotAIConfig.cpp b/src/PlayerbotAIConfig.cpp
index 37c74ac2..11c63372 100644
--- a/src/PlayerbotAIConfig.cpp
+++ b/src/PlayerbotAIConfig.cpp
@@ -684,9 +684,45 @@ bool PlayerbotAIConfig::Initialize()
     RpgStatusProbWeight[RPG_REST] = sConfigMgr->GetOption<int32>("AiPlayerbot.RpgStatusProbWeight.Rest", 5);
     RpgStatusProbWeight[RPG_OUTDOOR_PVP] = sConfigMgr->GetOption<int32>("AiPlayerbot.RpgStatusProbWeight.OutdoorPvp", 10);
 
+    // Playstyle profiles: per-personality weight tables (personality -> playstyle
+    // mapping lives in mod-ollama-chat's personality templates). Override any
+    // default with AiPlayerbot.RpgStatusProbWeight.<Profile>.<Status>.
+    {
+        struct ProfileDefault { char const* name; char const* confName; uint32 w[RPG_STATUS_END]; };
+        // weight order:            IDLE GRIND CAMP WRAND WNPC QUEST FLIGHT REST PVP
+        ProfileDefault const profiles[] = {
+            {"grinder",    "Grinder",    {0, 70, 15,  5,  5, 25, 10,  5,  5}},
+            {"quester",    "Quester",    {0,  5,  5,  5, 15, 90, 15,  5,  5}},
+            {"socializer", "Socializer", {0,  5, 15, 10, 60, 25, 15, 15,  5}},
+            {"explorer",   "Explorer",   {0, 10, 10, 50, 15, 30, 40,  5,  5}},
+            {"pvper",      "Pvper",      {0, 15,  5, 10, 15, 30, 10,  5, 60}},
+            {"idler",      "Idler",      {0,  5, 20, 10, 25, 15,  5, 40,  5}},
+        };
+        char const* statusNames[RPG_STATUS_END] = {"Idle", "GoGrind", "GoCamp", "WanderRandom",
+                                                   "WanderNpc", "DoQuest", "TravelFlight", "Rest", "OutdoorPvp"};
+        for (ProfileDefault const& prof : profiles)
+        {
+            std::unordered_map<NewRpgStatus, uint32>& weights = RpgStatusProbWeightProfiles[prof.name];
+            for (int s = RPG_GO_GRIND; s < RPG_STATUS_END; ++s)
+                weights[static_cast<NewRpgStatus>(s)] = sConfigMgr->GetOption<int32>(
+                    std::string("AiPlayerbot.RpgStatusProbWeight.") + prof.confName + "." + statusNames[s], prof.w[s]);
+        }
+    }
+
     syncLevelWithPlayers = sConfigMgr->GetOption<bool>("AiPlayerbot.SyncLevelWithPlayers", false);
     randomBotGroupNearby = sConfigMgr->GetOption<bool>("AiPlayerbot.RandomBotGroupNearby", false);
 
+    // Bot-initiated quest-help invites toward real players in good sentiment
+    // standing (mod-ollama-chat sentiment >= threshold). Chances are percent
+    // per 30-second eligibility check.
+    questHelpSentimentThreshold = sConfigMgr->GetOption<float>("AiPlayerbot.QuestHelpSentimentThreshold", 0.6f);
+    questHelpConfirmedChance = sConfigMgr->GetOption<float>("AiPlayerbot.QuestHelpConfirmedChance", 2.0f);
+    questHelpNearbyChance = sConfigMgr->GetOption<float>("AiPlayerbot.QuestHelpNearbyChance", 0.5f);
+    questHelpRandomChance = sConfigMgr->GetOption<float>("AiPlayerbot.QuestHelpRandomChance", 0.1f);
+
+    botAhSellChance = sConfigMgr->GetOption<float>("AiPlayerbot.BotAhSellChance", 25.0f);
+    botAhSellMaxListings = sConfigMgr->GetOption<uint32>("AiPlayerbot.BotAhSellMaxListings", 8);
+
     // arena
     randomBotArenaTeam2v2Count = sConfigMgr->GetOption<int32>("AiPlayerbot.RandomBotArenaTeam2v2Count", 10);
     randomBotArenaTeam3v3Count = sConfigMgr->GetOption<int32>("AiPlayerbot.RandomBotArenaTeam3v3Count", 10);
diff --git a/src/PlayerbotAIConfig.h b/src/PlayerbotAIConfig.h
index fa8a10de..eb47fb6f 100644
--- a/src/PlayerbotAIConfig.h
+++ b/src/PlayerbotAIConfig.h
@@ -371,10 +371,22 @@ public:
     bool autoDoQuests;
     bool enableNewRpgStrategy;
     std::unordered_map<NewRpgStatus, uint32> RpgStatusProbWeight;
+    // Alternative weight tables selected per bot by its mod-ollama-chat
+    // personality's playstyle (mod_ollama_chat_personality_templates.playstyle);
+    // bots without a playstyle use RpgStatusProbWeight
+    std::unordered_map<std::string, std::unordered_map<NewRpgStatus, uint32>> RpgStatusProbWeightProfiles;
     bool syncLevelWithPlayers;
     bool autoLearnQuestSpells;
     bool autoTeleportForLevel;
     bool randomBotGroupNearby;
+    // Bot-initiated quest-help invites (sentiment-gated, % per 30s check)
+    float questHelpSentimentThreshold;
+    float questHelpConfirmedChance;
+    float questHelpNearbyChance;
+    float questHelpRandomChance;
+    // Organic bot economy: bots list spare items on the AH near auctioneers
+    float botAhSellChance;        // % per check when a bot is near an auctioneer
+    uint32 botAhSellMaxListings;  // cap on concurrent listings per bot
     int32 enableRandomBotTrading;
     uint32 tweakValue;  // Debugging config
 
diff --git a/src/Script/Playerbots.cpp b/src/Script/Playerbots.cpp
index e9abd5b7..9974cb2b 100644
--- a/src/Script/Playerbots.cpp
+++ b/src/Script/Playerbots.cpp
@@ -33,6 +33,9 @@
 #include "PlayerbotCommandScript.h"
 #include "cmath"
 #include "BattleGroundTactics.h"
+#include "OngoingGuildRecruit.h"
+#include "PartyGuildFormation.h"
+#include "PersonalityGuildFactory.h"
 
 class PlayerbotsDatabaseScript : public DatabaseScript
 {
@@ -380,6 +383,9 @@ public:
     {
         PlayerbotWorldThreadProcessor::instance().Update(diff);
         sRandomPlayerbotMgr.UpdateAI(diff);  // World thread only
+        OngoingGuildRecruit::instance().Update(diff);            // World thread only
+        PersonalityGuildFactory::UpdateEmergentFounding(diff);   // World thread only
+        PartyGuildFormation::instance().Update(diff);            // World thread only
     }
 };
 
@@ -539,6 +545,7 @@ void AddPlayerbotsScripts()
     new PlayerbotsWorldScript();
     new PlayerbotsScript();
     new PlayerBotsBGScript();
+    AddSC_PartyGuildFormation();
     AddPlayerbotsSecureLoginScripts();
     AddPlayerbotsCommandscripts();
     PlayerBotsGuildValidationScript();
